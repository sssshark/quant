# 策略改进清单 (Improvements Backlog)

> 来源:与业界成熟策略对标后的差距清单。对标对象:
> Asness/AQR(动量与多因子)、Moskowitz-Ooi-Pedersen(时序动量)、
> Moreira-Muir(波动管理)、Daniel-Moskowitz(动量崩溃)、
> 桥水(风险平价)、AHL/Winton(CTA)、López de Prado(回测过拟合检验)。
>
> **共 78 条**:[高] 16(定位内应补)/ [中] 37(可选)/ [低] 22(超出当前定位)。含 2026-07-06 **J 节评审**新开 7 条(2高+3中+2低)——"跟主流策略比"的方法论盲区,多数 CUT ACROSS 已有条目(是对"已有结论被轻放"的再审视);含 2026-07-07 **F8** 多因子归因(扩 F4 CAPM,剥离被误判为 alpha 的债/金 beta);2026-07-08 新开 **M 节**多策略组合框架(B 型,3 条:M1 接口 + M3 国债时序动量 均落地 + M2 模块重组待办[低])

## 用法

- 解决一条就把 `- [ ]` 改成 `- [x]`,并在文末「解决记录」表补一行。
- 编号格式 `A1 / C2 / H1`,稳定不变,便于在 commit / PR / 日志里引用。
- 重新评估优先级时,直接改方括号里的 `[高]/[中]/[低]`,并在记录表说明原因。

## 图例

- **[高]** 定位内应补,优先做(决定回测可信度或几乎确定有效的"免费午餐")
- **[中]** 可选改进,按需
- **[低]** 超出当前定位 / 锦上添花

## 进度

```
总 78  ┃ 已完成 54 ┃ 待办 24        [高]0 [中]0 [低]24（J1–J5 全结案 + F8 多因子归因 + M1/M3 多策略(国债时序动量):[高]/[中] 清零,剩 [低] 均超定位/待实盘/阶段2）。完成明细见各条目
[高] 0（全清）  ┃ [中] 0（全清）  ┃ [低] 24
```
（每完成一批,更新上面三组数字)

> ⚠️ **重定基线（2026-07-05，三次演进）**：数据 bug 修复经历三轮，每轮都改变基线绝对数字：
>   ① **污染期**（G1 高阶矩 γ3=27 暴露）→ E3/E4 修复（fund_div 去重 + _SOURCE_SHIFT 截断 + DQ 守卫）
>      → 等权+全风控：年化 17.9%→13.4%、夏普 0.89→**1.15**、回撤 -24.8%→-17.6%。
>   ② **E3 修正 / pre_close 法**（B1 探测发现 pre_close 对拆分准确）→ 用 pre_close 列对齐除权/拆分，
>      替代 fund_div + _SOURCE_SHIFT → 年化 13.4%→**15.1%**、夏普 1.15→**1.20**、回撤 -17.6%→-20.2%。
>      E3 的 _SOURCE_SHIFT 是误判：把 510500/512100/513100 "pre_close 准但 close 断点"当作源永久断层截断、
>      丢失早期历史（513100 纳指丢 2013-2021 整段、512100 丢 2016-2022）；实测 pre_close 对除权/拆分准确
>      （close/pre_close 全期 max|ret|≤10%），改用它根因修复、恢复历史。详见 E3 条目。
>
> **✅ 重跑核验（以 ②pre_close 为权威，C1/C2 配对 block bootstrap n=2000）**：
> - **C1 反向波动**：Δ夏普 +0.039 / P=0.166（①时 +0.057/0.096），3/3 网格全胜但未达显著 → 保持默认 equal。
> - **C2 回撤控制**：Δ夏普 +0.051 / P=0.112（①时 +0.035/0.21）+ 回撤 -20.2%→-15.6%、Calmar 0.99；本职更强、更接近显著但未达 5% → 保持默认关。
> - **F3**：稳健。alpha=熊市避损（策略熊市 +4.4% vs 基准 -4.5%、回撤 -14.9% vs -52.2%）。
> - **G1**：更可信。等权 SR=1.198 DSR@50=0.961、反向波动 SR=1.234 DSR@100=0.949（①时 1.144/0.941）。
> - **B1 扩池**：稳健。扩 13 池 夏普 1.20→0.94、年化 -3.4pp、回撤不变；扩池有害保持原 7 只（①时回撤暴增 -17pp 是伪跳变放大）。
> - **A2**：Δ夏普 -0.030 / P=0.675（污染期 +0.13/P=0.39 是"降波动"伪改善），长窗口加权只降年化不提夏普，保持等权。
> - **B2**：分档 -1.2pp（货基 511990 低估真实 ~2%/年收益，方向不变），保持单一国债。
> - **总判**：F3/G1/B1 相对结论稳健；C1/C2 默认开关不变但显著性随基线漂移（C1 变弱、C2 变强）——再次印证"绝对值作废、需重跑"。

---

## 建议执行顺序(决定成败的 6 处,先做这些)

很多条目互相替代,真正"决定成败"的是下面 6 处——要么修正回测可信度,要么是几乎确定有效的免费午餐:

1. ~~**C1** 反向波动加权(代替等权)~~ ——已验证:3/3 网格全胜但 bootstrap P=0.17 不显著,保持默认 equal
2. ~~**C2** 叠加回撤控制~~ ——已验证:回撤本职强(-20.2%→-15.6%、Calmar 0.99)但夏普 P=0.12 不显著,保持默认关
3. ~~**A1** 动量崩溃保护~~ ——已验证:单一"大盘急涨"信号无中间甜点(THR=10% 踏空、≥15% 不触发),默认关结案
4. ~~**F3** 分 regime 表现~~ ——已完成:alpha 来源是熊市避损(策略熊市回撤仅基准 1/4);评估指标组(F1/F2/F3/F6)全部完成
5. **D1+D2** 统一成交口径 + 处理涨跌停——不修这俩,回测数字不可信
6. ~~**B1+B2** 扩池 + 分档防御~~ ——已验证有害(扩池撞 2019 后行业熊市、分档货基<国债),且选池有前视风险,保持原 7 只 + 单一国债

> **🔁 追加(2026-07-06 评审,J 节)**:**J1–J5 全部结案,[高]/[中] 清零,只剩 [低]**。① 两市一致(A股 12.7年 + 美股 23年):策略核心价值是**风控+分散**(砍回撤),**动量选股无可靠 alpha**(A股 P=0.148、美股 P=0.560)→ 策略本质是"风险管理的分散 ETF 组合"。② J4:回撤控制**跨债市 regime 稳健**(剔除十年债牛回撤仅恶化 +2.5pp),收益被债牛抬高 ~1.8pp/年、夏普 ~0.13,但熊债(−2%)下仍夏普 1.01——**非债牛幻觉**。③ J3:夏普可信度日频 DSR@50=0.960、**月频 n≈152 下=0.999**,两口径都 >0.95(原"日频偏乐观"假设被推翻)。④ **J5:冻结期(2022–2026)选股输给等权+风控(0.74<0.79、回撤还更差),PBO=0.67 警告被证实**——选股参数过拟合、'经验默认'没消除选择偏差;但风控层冻结期仍跑赢 B&H +0.55 夏普、过拟合集中在选股层。⑤ J2 落地:`hold_all` 已做成**实盘可选模式**(`live_config.json` 的 `hold_all` 键),切到等权全池+风控即可规避选股过拟合。**统一落地建议:实盘用 `hold_all=true`(等权全池+风控),放弃选股。**

---

## A. 信号与因子层 `momentum_core.py`

- [x] **[高] A1** 动量崩溃保护 — `decide_targets` 第五步,**默认关,验证后不建议启用**。触发效果(新浪,9次):后21日大盘涨6/跌3,真顶部(2015 股灾 5178 / 2024-09 / 2022-06)有救命但短期多踏空。**调参最终结论(THR×CUT 网格)**:THR=10%→夏普 0.96 但年化崩 12.5%(频繁砍仓踏空);THR≥15%→触发锐减、效果≈不开(=没用)。**无中间甜点**:单一"大盘急涨"信号在 A 股样本下要么过敏感踏空、要么不触发。保持默认关;要可用需多信号触发(急涨+估值高位+波动飙升),列入后续(结案:代码已实现且默认关,结论是不启用,不再单列)
- [x] **[高] A2** 混合动量各窗口等权平均 — **已实现+验证,保持默认等权**。`blended_momentum` 加 `weights` 参数(默认 None=等权,向后兼容)、`decide_targets`/`backtest` 加 `mom_weights` 透传;新增 `bootmom` 子命令(`bootstrap_mom_weights` 配对 block bootstrap)。**A/B + bootstrap 结论(tushare 12.7 年,全风控口径)**:长窗口加权((1,2,3)/(1,2,4 半衰期式)/(∝长度)/(1,1,3))**夏普 0.89→1.00~1.06 升,但年化 17.9%→12.4~13.0% 腰斩、波动 21.3%→12.8% 几近腰斩**;Sortino/Calmar/Omega 全降(1.99→1.38 / 0.72→0.53 / 1.34→1.23)。主候选(∝长度)Δ夏普 +0.13 但 **95%CI [-0.17,+0.47] 跨 0、P(Δ≤0)=0.39 不显著**。**关键判读**:夏普上升是"降波动"伪改善——长窗口主导使信号更黏、更偏防守(波动砍半主要砍的是上行),回撤几乎没降(-23.6% vs -24.8%)→ Calmar 反而更差。省一个过拟合自由度,保持默认等权,代码留作 A/B 工具(与 RISK_ADJ/C1 同类结案)。
  **【pre_close 重定 2026-07-05】** bootmom A/B 5 方案:等权年化 15.1%/夏普 1.20 最高;长窗口加权年化全降(14.5~15.0)、波动均≈12.7%(vol_target 统一压波动)、夏普 1.17~1.21。主候选∝长度 bootstrap Δ夏普 **-0.030** / P=0.675(污染期 +0.13/P=0.39 是"降波动伪改善",pre_close 重定后连这个假象都没了)。**更明确不支持启用**,保持默认等权
- [x] **[中] A3** `SKIP_RECENT=0` 默认不跳最近一月 — 位置:`momentum_core` L32 — **已验证(2026-07-05):保持 0**。扫描 skip∈{0,5,10,21,42} + 配对 block bootstrap:skip=21(经典 12-1)夏普 1.100 < skip0 的 1.198(Δ−0.097、P=0.718 即 72% 概率更差);skip=5 略升 1.229 但 P=0.356(CI 跨0、噪声内);无一显著优于 0。**经典 12-1 的 skip 在本框架失效**:① 多窗口(21/63/126)已含短窗,skip=21 致 21 日窗几乎失效;② ETF(非个股)短期反转弱、组合化平滑;③ 月度调仓已"跳过"月内噪声、与 skip 重叠;④ 波动目标+趋势过滤已处理短期波动。skip=42 回撤最优(-14.9%/Calmar0.92)但夏普不显著、年化降。**保持 SKIP_RECENT=0**(省自由度),与 C1/C2/A2 同类(理论改进实测不显著)
- [x] **[中] A4** `RISK_ADJ` 用最长窗口的日波动做分母 — **结案(2026-07-05)**:risk_adj 默认关(C1 验证不显著、保持不启用风险调整动量),优化一个关掉功能的分母口径无意义。若未来重评 risk_adj 再议
- [x] **[中] A5** 信号无统计显著性(t-stat) — **结案(2026-07-05)**:当前"动量>0 才持有 + top_n 排序"已是简化的显著性过滤(绝对动量为正=信号有效);Moskowitz t-stat 阈值引入新自由度(过拟合风险),非"动量轮动"定位必需
- [x] **[中] A6** 无信号确认(MACD/突破/多均线共振) — **结案(2026-07-05)**:多信号确认=多因子,超"纯动量轮动"定位(与 A8 同类);现有趋势过滤(200 日均线)已做大盘方向确认,加 MACD/突破是新自由度、过拟合风险
- [x] **[中] A7** 趋势均线只用 SMA — **已验证(2026-07-05):保持 SMA**。EMA(α=2/(N+1))vs SMA(200日):SMA 夏普1.198/回撤−20.2% 优于 EMA 1.102/−25.7%(Δ−0.095、P=0.911 即 91% 更差、回撤还加深)。**SMA 更稳**:EMA 对近期价敏感、震荡市频繁假信号;SMA(200日)更钝、符合"大盘趋势"慢变属性。KAMA/FRAMA 自适应均线同源(均比 SMA 灵敏),不另测
- [ ] **[低] A8** 单因子(动量),无价值/carry/质量 — 超出"动量轮动"定位
- [ ] **[低] A9** 无动量"加速度"(趋势的趋势) — 二阶信号作辅助过滤

## B. 标的池与资产配置

- [x] **[高] B1** 池太小(7 选 3),集中度高 — **已验证:扩池有害,保持原 7 只**。试加 7 只(上证50/恒生/标普/消费/医药/原油/中概→14 池):全期年化 17.9%→11.2%、回撤 -24.8%→-41.8%、夏普 0.89→0.59。**时段分割揭示伤害 80% 在 2019-2026**(前段 26.3 vs 22.9 接近、后段 10.3 vs 3.8 拉开),即医药(集采)/消费/中概/恒生集中熊市。**前视教训**:不能用全期回测收益挑池子成员(=用结果选原因);"原池更优"主要靠后段行业熊市,结论样本依赖。原 7 只(4 宽基+红利+黄金+纳指,全正收益)是经典时序动量资产选择,先验上更基础,保持。
  **【pre_close 重定 2026-07-05】** 扩 13 池(原油 tushare 不覆盖跳过,+上证50/恒生/标普/消费/医药/中概;拆分由 pre_close 法自动修复):年化 15.1%→11.8%、夏普 1.20→0.94、回撤 -20.2%→-20.3%(基本不变)。Δ夏普 -0.26、年化 -3.4pp。**结论与污染期一致:扩池有害,保持原 7 只**。形态变化:污染期扩池回撤暴增 -17pp 是伪跳变放大,清洁后回撤不变、危害纯为行业熊市标的(医药/消费/中概)拖累夏普。前视教训不变
- [x] **[高] B2** 防守资产单一(仅国债 511010) — **已验证:分档有害,保持单一国债**。试分档(动量≤0/vol_target 挪货基 511990、趋势/回撤挪国债):原 7 只下年化 17.9%→16.6%(-1.3pp)。根因:货基(~2%/年)收益 < 国债(2014-17 债牛+利率长下),21 月动量≤0 切货基少赚;同样样本依赖(国债恰好遇债牛)。先验上分档有流动性逻辑但样本不支持,保持单一国债。
  **【pre_close 重定 2026-07-05】** `decide_targets` 加 `defense_cash` 参数(第二/三步挪货基、第四/六步留国债,默认 None 向后兼容)。分档(货基 511990+国债) vs 单一国债:年化 14.0% vs 15.1%(-1.2pp)、夏普 1.11 vs 1.20。**分档有害,保持单一国债**(与污染期 -1.3pp 一致)。注:货币 ETF 511990 的 ~2%/年收益在份额结转(不在 close≈100、fund_div 返回 0 行),回测里货基≈0% 价格收益、低估真实;修正后分档伤害可能从 -1.2pp 降到 ~-0.5pp 但方向不变
- [x] **[中] B3** 池选择的存活偏差风险 — **结案(2026-07-05)**:B1 已充分论证——原 7 只(4 宽基+红利+黄金+纳指)是经典时序动量资产选择、先验基础;前视教训(不能用回测收益挑池)已记入方法论。universe 消融已量化 alpha 对池的依赖。无新可执行动作
- [x] **[中] B4** 缺低相关资产 — **结案(2026-07-05)**:B1 已证扩池有害(撞 2019 后行业熊市、夏普 1.20→0.94);加港股/日经/REITs/原油/铜等=扩池,同理有害。降相关应靠现有国债+黄金对冲,非扩池
- [x] **[中] B5** 未考虑标的流动性/容量 — **结案(2026-07-05)**:小资金定位下流动性/容量非瓶颈;F5 证成本极不敏感(Break-even 24.7x)、换手仅 7.4x。大资金场景才需考虑冲击成本,超出当前定位
- [ ] **[低] B6** 无行业/风格敞口拆分 — 全是 beta,没拆成长/价值/大小盘暴露

## C. 组合构建与风控

- [x] **[高] C1** 等权加权而非风险平价/反向波动 — 位置:`decide_targets` — **已实现(默认仍 equal)**:加 `weighting="inv_vol"` + `INV_VOL_WINDOW=63`。**mini-robust 结论(新浪不复权数据,方向可信)**:① 关 vol_target 后 inv_vol 夏普仍=等权(0.58=0.58)→ **否定"与 vol_target 重叠"假设**;② 参数网格 inv_vol 夏普≥等权仅 1/9(11%),与东财复权下 +0.02 结论相反、且都在 ±0.05 噪声内 → **C1 无可靠夏普提升、不稳健,保持默认 equal**。代码保留作 A/B 工具。
  **【pre_close 重定 2026-07-05（权威）】** review 3/3 vol_target 网格全胜(None/0.1/0.15: Δ+0.06/+0.04/+0.04)；配对 block bootstrap Δ夏普 +0.039 / CI[-0.04,+0.11] / P(Δ≤0)=0.166；夏普 1.20→1.24、回撤 -20.2%→-18.4%。**3/3 全胜但未达显著(P=0.17)**,保持默认 equal。注:①E3/E4 时 Δ+0.057/P=0.096,恢复早期历史后 C1 信号反而变弱
- [x] **[高] C2** 回撤控制 — `decide_targets` 第六步,默认关。**tushare 复权验证**:① 单独有效(关其它风控:回撤 -49.5%→-42.6%,夏普 0.54→0.59);② **但全风控下被 vol_target+trend 覆盖**(开/关 C2:夏普 0.81/0.83、回撤均 -25.6%,边际≈0)。与 C1 同类:单独有效但与现有风控重叠。保持默认关;若不用 vol_target/trend 可作替代降回撤。
  **【pre_close 重定 2026-07-05（权威）】** 全风控开/关 Δ夏普 +0.051、回撤 -20.2%→-15.6%(改善 4.6pp)、Calmar 0.75→0.99；bootstrap Δ夏普 +0.051 / CI[-0.02,+0.14] / P=0.112。**回撤/Calmar 本职强、夏普接近显著(单边11%)但未达5%**,保持默认关。注:①E3/E4 时 Δ+0.035/P=0.21,恢复历史后 C2 信号变强
- [x] **[中] C3** 无单标的集中度上限 — **已实现(2026-07-05)**。`decide_targets`/`backtest` 加 `max_weight` 参数(默认 None 关、向后兼容):个股权重超 max_weight 的部分挪防守资产。**实测**:None 回归基线(15.1%/1.198);cap=0.30(等权33%>30%触发)→ 年化14.0%/夏普1.224/回撤−18.2%(夏普略升、回撤改善,但年化降=降集中度的"降波动"取舍)。**默认 None 保持**(等权 top_n=3 下 max≈33% 已足够分散,cap 留作未来 inv_vol/集中 top_n 的防御性能力)
- [x] **[中] C4** 无尾部风险约束(CVaR/VaR) — **已实现(2026-07-05)**(归评估层,非风控)。`perf` 加 VaR95/VaR99(α 分位日损失)+ CVaR95/CVaR99(尾部条件期望);`_print_table` 加 VaR95/CVaR95 两列。**实测(全风控基线)**:VaR95 −1.12%/日、CVaR95 −1.96%、VaR99 −2.32%、CVaR99 −3.63%(尾部可控;最坏 1% 日均 −3.63%,ETF 组合可接受)。风控层不引入 CVaR 约束——已有 vol_target+趋势+C2(回撤)三重保护,CVaR 与之重叠且引入新自由度
- [x] **[中] C5** 相关性矩阵未显式建模优化 — **结案(2026-07-05)**:C1 反向波动(单标的波动)都不显著、保持 equal;协方差风险预算是更复杂版本(引入协方差估计噪声+优化),不会显著且增过拟合自由度。当前等权+波动目标已足够
- [x] **[中] C6** 两套减仓(波动+趋势)简单叠加 — 位置:`decide_targets` 第三/四步 — **已验证(2026-07-05):叠加不伤收益,两套互补、叠加最优**。4 配置 A/B(pre_close 基线,只动 vol_target/trend_ma):叠加 夏普1.198/回撤-20.2% > 仅vol 1.070/-28.0% > 仅trend 0.586/-49.2% > 都不开 0.534/-55.2%。配对 block bootstrap(n=2000):叠加 vs 都不开 Δ夏普+0.664 CI[+0.386,+0.913] P=0.000(两套总价值高度显著);加 vol 边际 Δ+0.612 P=0.000(vol 是主力,压波动 20.4%→12.8%);加 trend 边际 Δ+0.127 CI[-0.019,+0.289] P=0.043(trend 是补充,主要把回撤 -28%→-20%、Calmar 0.52→0.75;单边4.3%接近但未达5%)。**机制**:两套信号不同源——vol 看组合近期波动、trend 看大盘200日均线方向,同时触发=高波动+大盘下行=真危险期,多砍是对的;单套触发不过度。乘性叠加默认参数下已最优,**无需联合优化(省自由度,避免过拟合)**。保持默认 vol_target=0.15 + trend_ma=200 都开(确认部署口径正确;与 C1/C2/A2"验证后保持默认关"相反,本条是确认默认开)
- [x] **[中] C7** `VOL_WINDOW=20` 估计波动噪声大 — **已验证(2026-07-05):保持 20**。扫描 vw∈{10,20,40,60,90}:vw=20(默认)夏普1.198 最优;vw=60 夏普1.052(Δ−0.146、P=0.991 显著更差)、vw=40 也更差(P=0.951)。**长窗口反劣**:vol_target 是"波动超标即缩仓"、需快速响应,20 日灵敏;60/90 日滞后致躲高波动不及时。业界"60 日或 EMA 平滑"在"波动目标+月调"框架不适用
- [x] **[中] C8** 趋势过滤单向(只看跌破砍仓) — **结案(2026-07-05)**:月末重选已覆盖"突破回补"——大盘回升→动量转正→重新持有;trend 的职责是"大盘下行时减仓"(择时非个股信号),加"突破加仓"与月末重选重叠
- [ ] **[低] C9** 无止损(trailing/fixed stop) — 盘中黑天鹅只能等月末
- [ ] **[低] C10** 纯多头,无对冲 — 只能缩进国债,熊市不能做空盈利

## D. 调仓与执行

- [x] **[高] D1** 统一成交口径到 T+1 — **已实现**。核实发现 BT `set_coc` 与向量化 `shift(1)` **本质等价**(都是"月末当日收盘成交",原"shift(1) 更严谨"为误判)。两版统一升级到 **T+1 成交**(信号 T 日收盘 → 次日成交):向量化 `backtest` 加 pending 缓冲延迟写 weights、BT 删 `set_coc`(默认 next-bar 开盘成交)。消除"收盘价信号+收盘价成交"的乐观偏误。时序自检 6 项全过(见解决记录)
- [x] **[高] D2** 涨跌停处理 — **已实现(三处)**。共用 `_limit`/`limit_masks`(momentum_core:159915±20%、其余±10%)用未复权真实价算"收盘封板":向量化 `backtest` 加 `cant_buy/cant_sell` 掩码在成交日(T+1)过滤、BT `next` 加 `_at_limit` 下单前跳过、实盘 `build_orders` 加当日封板过滤。封板方向维持原仓、滞留权重落现金。tushare 路径用 fund_daily 的 pre_close 精确判定,akshare 回退用复权环比近似
- [x] **[中] D3** 滑点固定 5bp,未按流动性动态 — **结案(2026-07-05)**:F5 证成本极不敏感(Break-even 24.7x、翻 5 倍仍夏普>1),优化滑点精度(创业板/1000 多几个 bp)无意义。当前 5bp 固定已足够保守
- [x] **[中] D4** `paper_broker` 同步全成 — **结案(2026-07-05)**:H1 对账(reconcile)已覆盖执行偏差检测(调仓后实盘 vs target 偏离告警);paper 是离线测试用、同步全成合理,真实 qmt 部分成交/拒单由 H1 抓
- [x] **[中] D5** 整手向下取整 — **已实现(2026-07-05)**。`build_orders` 的 `int(...//LOT)` 向下取整改为 `int(.../LOT+0.5)` 四舍五入(五入),避免长期欠仓累积;`cash_buffer=0.99` 留 1% 现金兜底小幅超买。**实测**:12.5 lot 边界 → 1300(原向下 1200)。回测不受影响(向量化 backtest 用连续权重,整手仅实盘 build_orders 用)
- [ ] **[低] D6** 无拆单(TWAP/VWAP) — 小资金无所谓
- [ ] **[低] D7** 无订单成交确认/状态追踪 — 下单后不核成交

## E. 数据与样本

- [x] **[低] E1** 样本期单一(实际 2013-03-25~今 A 股) — **已审视(2026-07-05):受数据硬限制不可完全解决,降级为已知局限**。策略 ETF 池多数标的 2011-2013 才上市(510300 沪深300 2012-05、510500 中证500 2013-03、513100 纳指 2013、518880 黄金 2013-07、511010 国债 2013-03),起点被 BENCH+DEFENSE 钉在 2013-03-25(E5 审计实证 ready[0]=2013-03-25、起点时 POOL 已缺 512100/518880/513100)。2008 极端熊市时这些 ETF 不存在 → **无法把当前策略回测到 2008**;若强提前起点,POOL 多数未上市 → 动量凑不齐 top_n → 长期空仓扭曲统计。**现有 12.7 年已覆盖多 regime**:2015 股灾(5178→2638)、2018 贸易战熊、2020 疫情、2021-2024 核心资产调整;F3 已验证 alpha=熊市避损在三次基线均稳健。**未来方向**:跨市场验证(美股 ETF 跑同一套逻辑)可回答方法论泛化性,属独立工程、非当前策略延伸,暂不做。降级 [高]→[低](审视后判定不可完全解决、非"定位内应补"),结案
- [x] **[中] E2** 单一数据源 akshare — **已根治**:load_real 集成 tushare(`_load_via_tushare`),走 fund_daily+fund_div 手动前复权(pro_bar adj 对 ETF 不生效),配 `TUSHARE_TOKEN`/`TUSHARE_API` 环境变量自动启用(或本地 `.tushare_token` 文件回退),否则回退东财(带重试)。tushare 不限频、复权准确,是唯一稳定可靠的 ETF 复权源。**2026-07-05 SDK→HTTP 加固**:`_load_via_tushare` 原用 tushare SDK,本机 termux 编译 lxml 不过致 `import tushare` 即失败、real/review 子命令本机跑不通;改用服务商 HTTP POST(`_ts_post`,绕开 lxml 链),real/review 本机首次跑通
- [x] **[中] E3** 前复权跳变影响动量计算 — **已修(根因是 qfq 重复分红 bug)**。`_load_via_tushare` 原 tushare `fund_div` 对同一 ex_date 返回完全相同的重复行(510880 某日 8× 同 div_cash),逐条施加复权 → 历史价被过度下压 → 除权日伪跳变(+45% 等)。修复:按 ex_date 去重保留一条(重复行=同一笔分红被重复记录)。同时 `_SOURCE_SHIFT` 截断 510500/512100/513100 的 tushare 源永久断层(无分红/拆分记录、当时判不可修复)。**【2026-07-05 修正】** `_SOURCE_SHIFT` 是误判:这 3 只的 pre_close 对拆分实际准确(close/pre_close 全期 max|ret|≤10%),close 断点可修、非源永久损坏。已改用 pre_close 法(见重定基线说明②)替代 fund_div+_SOURCE_SHIFT,恢复这 3 只早期历史(513100 2013-2021 等);同时覆盖 159928/513500/512010 等份额拆分(fund_div 不记录)。DQ 守卫清洗后 0 伪迹
- [x] **[中] E4** 无数据质量监控 — **已实现**。`_data_quality_guard(px)`:清洗后若仍残留不可成交伪迹(|日收益|>1.5×涨跌停)大声告警。`load_real` 末尾自动调用,正常清洗后返回空清单;非空=还有未发现源 bug 需人工核查。正是这次实现 G1 时的高阶矩校验(γ3=27)暴露了 E3 的埋藏 bug
- [x] **[中] E5** 起点锚定逻辑 — 位置:`load_real` → `_anchor_start` — **已审视(2026-07-05):无硬前视**。`ready[0]`=BENCH(510300)+DEFENSE(511010)首个共同非NaN日,ffill 不改首日 → 纯数据首日驱动、不依赖收益;backtest 全链路防未来函数(`recent=loc[:d].dropna`+`w_lag=shift(1)`+pending T+1 成交+warm-up 跳过 `need` 日,D1/D2 时序自检 6 项全过)。微弱"前视"仅存方法论层:BENCH/DEFENSE 人为选定、起点随资产选择间接固定(见 B1 前视教训),代码层无法根除。截到 `ready[0]` 是有意设计(保证起点核心池就绪;若提前则 POOL 多数未上市→动量凑不齐 top_n→长期空仓扭曲统计,且等于又一轮重定基线、方向错误)。**补透明化**:抽 `_anchor_start(px)`(tushare/东财两处共用、DRY),打印 `[E5 起点] ready[0]=…, 起点时 POOL 未就绪:[…]`——把"早期样本由先上市标的主导"这一隐性事实显性化(实测起点 2013-03-25 时 512100/518880/513100 未上市),便于审计/复现/未来 E1 起点敏感性。（编号 E5 专指本条;重定基线事件以 `pre_close 重定` 标记、见顶部说明）
- [ ] **[低] E6** 无分红/税收处理 — 总收益略偏离真实
- [ ] **[低] E7** 停牌 ffill 未标记 — 合理但可标记区分

## F. 评估指标 `perf()` `etf_momentum.py` L117

- [x] **[高] F1** `perf()` 仅 5 指标,缺 Sortino/Calmar/Omega — **已实现**:`perf()` 加 Sortino(下行偏差作分母、MAR=0)、Calmar(年化/|最大回撤|)、Omega(正负收益和之比、阈值0,不年化),`_print_table` 加三列。无风险/MAR/阈值统一按 0(与现有夏普口径一致)。**新指标揭示夏普掩盖的信息**:12.7 年真实数据下反向波动 C1 夏普低于等权(0.81<0.89)但 Sortino/Calmar/Omega 均更高(2.22>2.00 / 0.74>0.72 / 1.37>1.34)→ 反向波动"下行更稳",印证 C1 不稳健(不同指标下结论分歧、边际在噪声内,仍保持默认 equal)
- [x] **[高] F2** 缺滚动指标(滚动夏普/滚动回撤) — 全期一个数掩盖"某段靠运气",与过拟合哲学矛盾 — **已完成**:新增 `rolling_metrics()`(滚动年化夏普/波动)+ main 三子图。反向波动滚动夏普均值 0.96/最差 -1.64/>0 占比 84%,优于等权 0.92/-1.72/81%
- [x] **[高] F3** 缺分 regime 表现(牛/熊/震荡) — **已实现**:`regime_labels`(基准沪深300 回撤>20%=熊、站上 200 日均线=牛、其余震荡,刻画市场状态与持仓无关)+ `run_regime`(按 regime 切片 `_seg_perf` 重建净值,带基准同 regime 对照);新增 `regime` 子命令 + real 末尾两次拆解(等权/反向波动)。**揭示 alpha 来源**:策略熊市年化 +4.4%(基准 -4.5%)、回撤 -14.9%(基准 -52.2%,仅约 1/3.5)——全期夏普 1.20 看着平庸,但收益几乎全来自熊市避灾;牛市年化 12.1% 略输基准 13.6% 但夏普 2.26>2.03(回撤控制更好,轮动切换成本);震荡微正(Sortino 0.22)。反向波动熊市年化 +4.7% 优于等权 +4.4%。**【pre_close 重定 2026-07-05】** 数字为 pre_close 基线(①E3/E4:熊市 +2.8%、全期夏普 1.15、牛市夏普 2.45、震荡 Sortino -0.23;污染期:熊市 +1.3%/-13.1%);相对结论(alpha=熊市避损)三次基线均稳健
- [x] **[中] F4** 缺因子归因(alpha-beta 分解、IR) — **已实现(2026-07-05)**。新增 `factor_attribution(daily, bench_daily)`:策略日收益对沪深300 做 CAPM 单因子回归,纯 Python OLS(beta=cov/var、alpha=Jensen 截距、IR=主动收益/跟踪误差、R²=corr²),年化口径与 perf 一致(自然日)。新增 `run_attribution` + `attrib` 子命令(全风控口径 vol+trend)。**实测(pre_close 基线)**:策略年化 15.1% vs 基准 7.4%、超额 +7.8%、**beta=0.37**(低市场暴露,大量时间持国债+波动缩仓)、**年化 alpha +11.8%**(Jensen 纯超额,策略核心价值)、IR=0.33(中等,未达 0.5 优秀)、R²=0.41/相关性 0.64。**分解**:超额 +7.8% ≈ alpha +11.8% + beta 拖累 −4.6%(低 beta 致基准涨时少赚,靠 alpha 补回——与 F3 互证:alpha=熊市避损,平时低 beta 主动放弃部分上行换回撤控制)。CAPM 单元测(n=5000 合成序列)回收 beta 0.483(真值0.5)、alpha 10.0%(真值10.1%),数学正确
- [x] **[中] F5** 缺换手率与成本敏感性 — **已实现+验证(2026-07-05)**。`backtest` 加 `return_turnover` 参数(默认 False 向后兼容)暴露逐日换手,补换手率统计。**实测(pre_close 基线)**:年换手 **7.4x**(月均 0.61x,月度调仓、换手中等偏低)。成本倍数扫描:基线 7.5bp(费2.5+滑5)→ 夏普1.198/年化15.1%;翻倍(15bp)→1.154/14.4%(夏普仍96%);5x(37.5bp)→1.020/12.6%(仍>1)。**成本拖累仅 0.63%/年**(毛15.7%→净15.1%)。**Break-even ≈24.7x**(单边185bp 才吃光 alpha)。**结论:策略对成本极不敏感**——F4 的 alpha+11.8% 不是成本幻觉,翻 5 倍成本仍夏普>1;回撤几乎不受影响(-20.0%→-20.9% at 5x,成本只小幅日扣、不改变持仓)。低换手(7.4x)×低成本(7.5bp)×大alpha(11.8%)= 成本安全边际极大
- [x] **[中] F6** 净值图无水下曲线(underwater plot) — 回撤持续期/恢复期不可视化 — **已完成**:三子图加水下曲线(等权 vs 反向波动回撤对比可视化)
- [x] **[低] F7** 基准单一(只沪深300) — **已实现(2026-07-05)**。新增 `bench_6040(px)`(60% 沪深300+40% 国债,经典被动组合);universe 消融表加"股债60-40"对照行。**实测**:60-40 夏普0.54/年化6.2%/回撤−28.1%(优于纯沪深300 夏普0.44/回撤−45.5%,债券缓冲降回撤);策略 1.20 远超两被动基准。中证全指跳过(无对应 ETF、拉指数超出数据源)
- [x] **[中] F8** 因子归因只到 CAPM,多资产 alpha 被高估 — **已实现(2026-07-07)**。F4 的 CAPM 只对沪深300 回归,而策略大量持国债/黄金/纳指,CAPM 会把这些**非权益收益误判成 alpha**(F4 自身已有症状:beta=0.37 低暴露但 alpha+11.8%>超额+7.8%)。新增 `factor_attribution_multi(daily, px)`:对 6 因子做 OLS——MKT(沪深300)/SMB(中证1000−300)/VMG(红利−300)/BND(国债)/GLD(黄金)/NSDQ(纳指−300),全部从已加载 `px` 构造(`_SOURCE_SHIFT` 是死代码、pre_close 法已恢复早期历史,6 因子全可用);含截距 OLS(`np.linalg.lstsq`)+ 系数 t-stat(`pinv(X'X)·σ²ε`)+ 调整 R² + 因子相关矩阵。`run_mfattribution` + `mfat` 子命令(与 `attrib` 同源全风控口径)。**实测(tushare n=3100)**:CAPM 年化α **+11.7%** → 多因子年化α **+7.0%**,被「剥离」**+4.7%**(CAPM 误归为 alpha 的债/金/风格 beta)。因子贡献:MKT β=0.55(+5.4%/yr)、NSDQ β=0.13(+1.5%)、GLD β=0.13(+1.4%)、BND β=0.22 t=3.2(+0.7%)、SMB β=0.13(−0.7%)、VMG β=0.07(≈0);R² 0.41→0.53,无 |corr|>0.7 共线性。**判读**(比"alpha 全假"更细):CAPM 高估了 ~40%(债/金/纳指/小盘暴露被当 alpha),但 **+7.0% 真 alpha 在 6 因子控制下存活**——风控/择时(vol_target+趋势+月度轮动)在静态资产暴露之外仍有真实附加值,印证 F3"alpha=熊市避损"非纯 beta。**残留 caveat**:6 因子只剥离**资产类别** beta,未含**风格/行为**因子(动量/低波);策略本身是动量+低波属性,若加 TSMOM/低波因子 alpha 可能再缩——但本条定位是"扩 CAPM 到多资产因子",7.0% 是"扣资产暴露后"的诚实下界,非"纯 skill"点估计。单元测 2 个:`test_factor_attribution_multi_recovery`(回收 β/α)+ `test_mfat_strips_spurious_alpha`(核心论点:真 α=0+债金 beta → CAPM 假阳性 +3~5%、多因子剥回近 0)。**(关涉 F4/F3)**

## G. 过拟合检验(已有基础,可加严)

- [x] **[中] G1** Deflated Sharpe Ratio — **已实现**。`deflated_sharpe(daily, n_trials)`(Bailey-López de Prado 2014;纯 Python `_norm_ppf` Acklam + `_norm_cdf` erfc,免 scipy)+ `dsr` 子命令。**清洁数据结论(tushare 12.3 年)**:①E3/E4 时 等权 SR=1.15 DSR@100=0.91/@50=0.94,反向波动 SR=1.20 DSR@100=0.93;**②pre_close 重定后** 等权 SR=1.198 **DSR@50=0.961(可信)、@100=0.935(边际)**,反向波动 SR=1.234 **DSR@50=0.971、@100=0.949**——恢复早期历史后 DSR 全面升、更可信。γ3=-0.98/γ4=15.6(合理负偏+肥尾)。**G1 的高阶矩校验正是暴露 E3 数据 bug 的入口**——污染版 DSR 曾虚高至 0.985(伪正偏压低 σ)。与 boot/wf 互补:单项改进不显著、核心策略经得起多重比较
- [x] **[中] G2** 缺 Purged/Combinatorial CV — **已实现(2026-07-05)**。新增 `purged_cv`(López de Prado《AFML》):时间均分 6 折非重叠,轮流每折 test、其余 train,但 **purge**(test 边界 ±21 日,防动量标签泄漏)+ **embargo**(test 后 21 日,防信号经滚动窗泄漏到后续 train);train 在 WF_GRID 选参、冻结到 test 评估,拼 6 折 OOS。新增 `pcv` 子命令。**实测**:全样本内 grid 最优夏普 1.24,样本外(6 purged 折、12.7 年)年化 13.4%、夏普 **1.11**、回撤 -20.1%,**夏普衰减 0.89**(≥0.6 阈值,远超)→ 策略经得起 AFML 最严样本外检验、无显著过拟合。各折选参多为部署默认(lookbacks/top_n/trend_ma 稳健中心,仅 trend_cut/risk_adj 因段而异),非"全压一个过拟合参数"。与 G1(DSR@50=0.961)、wf 三重过拟合检验互证
- [x] **[中] G3** 缺 PBO(backtest overfitting 概率) — **已实现(2026-07-05)**。新增 `pbo`/`run_pbo`+`pbo` 子命令(Bailey-LdP 2017 排名法:purged K-fold 每折 ISC 最优参数在 test 的排名,落下半占比=PBO)。**实测:PBO=0.67(>0.5,过拟合警告)**——各折 ISC 最优在 test 排名 [4,2,6,6,5,3],4/6 落下半。**关键洞察**:与 pcv/wf 衰减 0.89 不矛盾——策略整体稳健(8 个 grid 参数样本外都还行),但"选参"有过拟合(样本内最优≠样本外最优)。**印证当前"用经验默认、不 grid 挑参"的正确性**:若用 grid 挑最优会过拟合;部署参数(lookbacks=21/63/126/top_n=3/vol_target=0.15/trend_ma=200)是经验值非挑出,避开此坑。与 G1(DSR)、wf、pcv 四重检验形成完整图景(整体稳健 + 选参勿信)
- [ ] **[低] G4** `robust` 用 one-at-a-time — 缺二维交互扰动
- [ ] **[低] G5** `wf` grid 仅 8 个 — 覆盖有限
- [x] **[低] G6** `boot` 只检了 RISK_ADJ — **结案(2026-07-05)**:实际已对关键改进做配对 block bootstrap——C1(inv_vol)、C2(drawdown)、A2(mom_weights)、C6(叠加)、A3(skip)均 n=2000 bootstrap。boot 子命令虽只跑 RISK_ADJ,其他改进已在各自验证时检验过

## H. 工程与运营

- [x] **[高] H1** 无回测-实盘对账(reconciliation) — 机构标配;实盘偏离回测无预警 — **已实现(2026-07-05)**。新增 `reconcile(target, positions, total, price, thr=RECONCILE_THR=0.10, when=)`:模型目标权重与实盘持仓换算到同一口径(含 `__cash__` 现金币种)逐币种比,偏离 ≥10% 总资产告警(阈值 > `REBALANCE_BAND=0.05`,避免整手取整/静默区固有偏离误报)。`etf_momentum_live.main` 两个天然对账点接入:**调仓前**(实盘累积持仓 vs 当前模型 target → 抓跨月漂移/上次调仓未完整执行/分红或手工操作)、**调仓后**(刚执行持仓 vs 本次 target → 抓涨跌停/拒单致下单未全成交,实盘最关键);paper 与 qmt 两模式都接。只读对比、不改交易、不抛异常(对账失败不阻断调仓);返回最大偏离便于落日志。单元测三场景(一致 max0.0% / 偏离 510300低配-13pp+511010超配+14pp / 空仓 现金+99pp)全过。**局限**:paper 全成交→对账总≈一致(验逻辑用);真实价值在 qmt 实盘接入后(订单在途/拒单偏离);对账结果当前仅 print,持久化日志属 H7
- [x] **[高] H2** 凭证硬编码 — **已实现**:`etf_momentum_live.py` 的 `QMT_PATH/ACCOUNT_ID` 改三级回退(环境变量 `QMT_PATH`/`QMT_ACCOUNT_ID` > 本地 `live_config.json` > 占位值),经 `_load_live_secret` 读取、不写进源码;`live_config.json` 加 gitignore(样例 `live_config.example.json` 入库)。占位值时 `connect_trader` 拒绝连真实账户(防误操作)。与 `.tushare_token` 同一约定
- [ ] **[低] H3** 单文件脚本,无框架抽象 — 降级(2026-07-05)[中]→[低]:大重构(信号/组合/执行分层)风险大、收益不明,当前单文件可工作;超出"验证策略"定位,留待多策略时再做
- [ ] **[低] H4** 无监控/告警 — 降级(2026-07-05)[中]→[低]:H1 对账(reconcile)已覆盖核心告警(实盘偏离回测);全面监控(断连/数据错通知)超当前 paper 阶段,qmt 实盘接入时再补
- [x] **[中] H5** 无单元测试 — **已实现(2026-07-05)**。新增 `test_momentum.py`(6 个测试:G1 norm 往返、F4 CAPM 回收、F1 Sortino 钳制、C4 VaR/CVaR、H1 reconcile、D5 整手四舍五入);`python test_momentum.py` 6/6 PASS,pytest 兼容。把历次自检脚本固化为回归保护——改 perf/backtest/reconcile/build_orders 等会被立即捕获
- [ ] **[低] H6** 参数硬编码 `momentum_core` — 降级(2026-07-05)[中]→[低]:参数→配置文件是工程优化,当前常量可工作、实验已能通过 backtest 参数透传做;非策略改进
- [ ] **[低] H7** 用 `print` 无日志系统 — 缺分级日志、无落盘
- [ ] **[低] H8** 无实验跟踪 — 无 MLflow/wandb 式版本化
- [ ] **[低] H9** 状态文件无并发保护 — `paper_account.json` 并发写有竞态
- [ ] **[低] H10** 无回滚机制 — 下单出错无补救
- [ ] **[低] H11** 无绩效/归因报告自动生成 — 无 PDF/HTML 月报

## I. A 股本土化与制度

- [x] **[中] I1** 未对比本土有效因子 — **已对比(2026-07-05)**。ETF 框架下严格因子(反转/低波需个股数据)做不了,对比"动量轮动 vs 本土被动选择":动量轮动 15.1%/夏普1.198/回撤−20.2% **显著优于** 等权全池 11.0%/0.701/−34.7%、沪深300 7.4%/0.442/−45.5%、60-40 6.3%/0.542/−28.1%、**中证1000小盘 2.1%/0.207/−51.0%**(最差)。**关键反常识**:所谓"A 股小市值因子强"在 ETF 层面/近13年不成立——中证1000 被动持有年化仅2.1%、回撤−51%(长期阴跌)。动量轮动相对等权全池多 +4.1pp 年化 = 选股+风控的纯附加值。严格反转/低波因子对比需个股层面数据(超出 ETF 轮动定位),本对比足够回答"动量轮动 vs 本土被动选择"
- [x] **[中] I2** T+1 制度约束未体现 — **结案(2026-07-05)**:D1 已实现 T+1 成交口径(信号 T 日收盘→次日成交,消除"收盘价信号+收盘价成交"乐观偏误);月末调仓+T+1 已合规。极端行情调仓受限是月度调仓固有取舍,非缺陷
- [ ] **[低] I3** 未考虑 ETF 折溢价(IOPV 偏离) — 折价买/溢价卖是额外收益源
- [ ] **[低] I4** 程序化交易报备仅注释 — 代码无报备状态检查(合规)

---

## J. 与主流策略对标的方法论盲区(2026-07-06 评审)

> 来源:对整套策略做"跟主流量化比"的复核(对标 AQR/Moskowitz/Moreira-Muir/Daniel-Moskowitz/LdP 的方法论)。
> **核心结论**:验证工具链(DSR/PBO/PCV/WF/bootstrap/涨跌停/T+1)远超普通散户,但回测夏普 1.2 **几乎肯定被三个结构性偏差抬高**——① 池前视、② 风控为主非选股、③ 防守资产吃十年债牛;而项目自己的最严检验(PBO=0.67 / DSR@100=0.935 / B1 消融)**其实已报警、被合理化掉**。本节把这些盲区显性化。多数 CUT ACROSS 已有条目(是对"已有结论被轻放"的再审视),非独立新功能;每条给"行动"而非"结论"。

- [x] **[高] J1** 标的池前视 —— 池里 `518880 黄金` + `513100 纳指` 是 2014–2025 的**事后赢家**,纳入池=用结果选原因。B1 的"踢掉它们夏普降"是**循环论证**(已知涨→踢必降),洗不清前视。真正的 prior-based A 股动量池应是 4 宽基+红利;黄金/纳指是分散化赌注、恰好兑现。**这是实盘夏普最可能腰斩的单一原因。** 唯一根治 = 跨市场样本外验证(美股/港股 ETF 池跑同逻辑);E1 把跨市场验证标"超定位"实为回避本条。**(关涉 B1/B3/E1)**
**【已验证(2026-07-06,美股 23 年,miniconda+yfinance 取数)】** `crossmarket.py` 跑通:美股池(SPY/QQQ/IWM/EEM/GLD/VNQ + TLT 防守,按资产类别选非按收益),SPY+TLT 起点锚定 2003,**23.0 年 / 277 次调仓 / 参数全沿用 A 股部署值零调整**(真样本外)。**结果(夏普 / 最大回撤)**:等权全池(不风控) 0.70 / −49.7% → 等权+vol+trend(不选股) **0.84 / −31.7%** → 动量轮动 top3+风控 **0.83 / −33.4%**;SPY 买入持有 0.67 / −55.2%。
  **① 风控(vol+trend)跨市场成立、是核心价值**:加风控把等权持有回撤从 −50% 砍到 −32%、夏普 0.70→0.84(与 A 股 C6/J2 同构,机制可泛化)。
  **② 选股跨市场无效**:动量轮动 0.83 ≤ 等权+风控 0.84(Δ−0.01),配对 block bootstrap **P=0.560 不显著**——与 A 股 J2(P=0.148)一致,**两个独立市场都确认"选 top3"无可靠 alpha**。
  **③ 策略跑赢 SPY 买入持有(+0.16 夏普)** 但纯靠风控降回撤(−33% vs −55%)、非选股——与 A 股 F3"alpha=避熊"同源。
  **对 J1 原问题的回答**:策略逻辑能泛化(风控+分散),A 股结果**非纯池前视**;但"能泛化的那部分"是通用风险管理、与池子无关,而真正的"动量选股"在两市都是噪声。**净结论:策略本质是"风险管理的分散 ETF 组合",不是动量策略**;J1 池前视担忧对"风控部分"解除、对"选股部分"本就无物(选股从未有效)。代码:`crossmarket.py` + `backtest`/`bootstrap_selection` 的 `trend_code` 口子 + `test_crossmarket_globals_swap`。
- [x] **[高] J2** "动量轮动"选股边际 —— **已验证(2026-07-06,加 `nomomentum` 子命令)**:对照"等权全池 + vol_target + trend、不选股"(`decide_targets` 加 `hold_all` 分支,候选集与策略同时点一致,只差"选不选")vs 部署动量轮动,同口径同执行(T+1 + 涨跌停)。**结果(tushare 12.7 年)**:对照 年化 11.5% / 夏普 **1.02** / 回撤 −16.9% vs 策略 15.1% / **1.20** / −20.2% → Δ夏普 +0.18、年化 +3.6pp,但**回撤更差(−16.9%→−20.2%,集中 top3 抬高回撤)**;配对 block bootstrap Δ夏普 +0.177 / CI[−0.14,+0.48] / **P=0.148 不显著**。**2×2 全景(夏普;选股×风控)**:不选股+不加风控 ~0.70(I1) / 选股+不加风控 0.53(C6都不开) / 不选股+加风控 **1.02**(J2) / 选股+加风控 **1.20**(部署)→ 横跨(风控)+0.32~0.67 ≫ 竖跨(选股)−0.17~+0.18:**风控是主力引擎,选股是噪声级、无风控时反而拖累**。**判读(修正原假设)**:① 选股**不是零贡献**(+0.18),但**不可靠**(噪声内,与 C1/C2/A2 同类);② 策略核心价值来自"分散池 + vol_target + 趋势"——对照单跑夏普 1.02 已远超 B&H 沪深300 0.44,选股只是噪声级额外;③ 选股还牺牲了回撤(等权全池回撤更浅)。**结论**:部署策略相对"等权全池+风控"无可靠选股 alpha;若重风险调整,等权全池版(夏普 1.02 / 回撤 −16.9%)是更稳的近替代。改进方向应转向"持有什么/风控"而非"选谁"。代码:`nomomentum` 子命令 + `decide_targets` `hold_all` 参数 + `test_hold_all` 单元,留作 A/B 工具。 → 进一步(2026-07-06):`hold_all` 已做成**实盘可选部署模式**——`momentum_core.HOLD_ALL` 常量 + `live_config.json` 的 `hold_all` 键(见 `etf_momentum_live._load_hold_all`),改配置即可在'动量轮动'与'等权全池+风控(稳健版)'间切换,无需改代码。**(关涉 C6/F4)**
- [x] **[中] J3** DSR/bootstrap 的有效 N —— **已验证(2026-07-06,`run_dsr` 加月频口径)**。把策略日收益重抽样成月频(月内复利,n≈152)再算 DSR,对比日频(n≈3225)。**结果(等权策略 DSR@N=50)**:日频 0.960 → 月频 **0.999**(+0.039);月频 N=10/50/100 全 0.999、日频 0.993/0.960/0.933。
  **判读(原假设被推翻)**:J3 原担心"日频夸大自由度、数字偏乐观"——实测**相反**,月频 DSR 反而更高。机制:① 月收益聚合后接近正态(日收益肥尾 γ4≈15.6 被 CLT 平滑),非正态修正项 (g4−1)/4·SR² 变小 → σ(SR̂) 变小 → SR_max 变小(日频 0.674 → 月频 0.519)→ DSR 升;② 月频观测年化夏普 1.258 略高于日频 1.193(策略月度调仓、月收益是自然粒度)。
  **净判**:策略夏普 ~1.2 在日频和月频两口径下都 >0.95 可信,经得起更严的月频检验。这与 J1/J2/J4(质疑"edge 是什么/稳不稳")不矛盾:J3 回答"夏普数字真不真",答案是**真**。
  **残留 caveat**:月频 n=152 仍把每月当独立,而 edge 按 F3 集中在 3 个熊市 episode——真正的"regime 独立观测数"更小;但月频已是频率层最诚实的口径,regime 独立性需独立样本佐证(已由 J1 美股 23 年提供)。代码:`_monthly_returns`/`run_dsr` 双口径。**(关涉 G1/F3)**
- [x] **[中] J4** 国债防守资产债牛敏感性 —— **已验证(2026-07-06,`bondstress` 子命令)**。真实国债 511010 在样本期 **+2.9%/年**(明显债牛)。把防守资产收益替换为 0%/年(剔除债牛)、−2%/年(熊债)重算:**结果(夏普 / 回撤 / Calmar)**——真实 **1.20 / −20.2% / 0.75** → 0%(平价) **1.07 / −22.8% / 0.58** → −2%(熊债) **1.01 / −23.5% / 0.53**。
  **① 回撤控制机制稳健**:剔除债牛后最大回撤仅恶化 +2.5pp(−20.2%→−22.8%)、熊债 +3.3pp——机制靠"熊市挪进不跌的防守"(防守不跌即可避险),与防守资产涨不涨无关。**原假设"机制整体失灵"被推翻**。
  **② 收益/Calmar 水平有债牛顺风**:债牛贡献 ~1.8pp/年 年化、~0.13 夏普;Calmar 0.75→0.58→0.53(随 CAGR 等比例降,因回撤基本不动)。但熊债下策略仍 12.5%/夏普 1.01,仍远超 B&H 沪深300(0.44)。
  **净判**:J4 担忧"Calmar regime-contingent"对**水平**部分成立(绝对值被债牛抬高 ~0.1–0.2 夏普、~1.8pp 年化),对**机制**不成立(回撤控制跨债市 regime 稳健)。策略不是债牛幻觉,但应预期非债牛 regime 下夏普低 ~0.1–0.2。代码:`run_bondstress`/`_synth_defense_series`/`bondstress` 子命令。**(关涉 B2)**
- [x] **[中] J5** PBO=0.67 再审视 —— **已验证(2026-07-06,`freezetest` 子命令)**。把连续跑的策略在 2022-01-01 切开,看"近期未被全样本 A/B 直接优化"的冻结期(2022–2026,~4.5 年)。**结果(夏普 / 回撤)**:全样本 动量轮动 1.20/−20.2% vs 等权+风控 1.02/−16.9%;**冻结期** 动量轮动 **0.74/−15.0%** vs 等权+风控 **0.79/−10.8%**;沪深300 B&H 冻结期 0.18/−32.7%。
  **① PBO=0.67 警告被证实**:冻结期选股**输给**等权+风控(0.74 < 0.79、Δ−0.06,回撤还更差 −15.0% vs −10.8%)——全样本调出来的选股在近期段不成立,"用经验默认值"没消除选择偏差(整份 backlog 就是 A/B 选参记录,PBO 惩罚的正是这种选择)。
  **② 过拟合集中在选股层,风控层稳健**:冻结期策略(两版本)仍跑赢 B&H +0.55~0.61 夏普、回撤砍到 1/3(−32.7%→−11~15%)——vol_target+trend 跨段稳健(与 WF 衰减 0.89、J4 跨 regime 一致)。
  **③ 绝对夏普回落**:全样本 1.20 → 冻结期 0.74(选股)/0.79(等权),与 J4 债牛顺风、edge 均值回归一致;但仍稳正、远超 B&H。
  **④ DSR@100=0.935 担忧**已由 J3 化解(月频 DSR@100=0.999)。
  **落地(与 J2 一致)**:别信选股参数样本外表现 → 切 `hold_all` 实盘模式即可规避选股过拟合、保留稳健风控层。**严格说无真样本外**(backlog 用到 2026 全样本),冻结期是近期段近似,真样本外须等实盘向前跑出。代码:`run_freezetest`/`freezetest`。**(关涉 G3/G1)**
- [ ] **[低] J6** 无实盘 track record —— H1 对账在 paper 模式恒一致,验证不了真实执行 gap(滑点/部分成交/QMT 在途)。纸面再好须实盘 6–12 个月见真章。**(关涉 H1/D4)**
- [ ] **[低] J7** ETF 折溢价未建模 —— `513100 纳指` QDII 盘中溢价常 3–5%,月末收盘调仓可能买在溢价高点,对"轮动到纳指"是实打实隐性成本。(与 I3 同源,重申)

> 判读:J1/J2 是"决定夏普能不能信"的硬伤(定位内应补);J3/J4/J5 是"数字偏乐观、需打折"的方法论修正;J6/J7 是工程跟进。**不推翻策略,但要求把"1.2 夏普"当作上界而非点估计对待。**

---

## M. 多策略组合框架(B 型)(2026-07-08 起)

> 方向演进:从"单一 ETF 动量策略"→"多策略组合"。CTA 作为一个独立策略,未来叠加 carry / 均值回归 /
> 海外配置等,上层 `MultiStrategy` 做资金分配。**非**个股横截面多因子选股(A 型)。
> 设计原则:**单策略内部不做多因子**(瘦策略 = 单 alpha + 自带风控),多 alpha 来源 = 多个独立
> `Strategy` 在组合层叠加(避免 PBO 0.67 的过拟合教训);CTA 内部 vol/trend/crash/drawdown 是
> **风控,不是因子**。

- [x] **M1** 多策略接口(阶段1,2026-07-08 落地)— 新 `multi_strategy.py`:`Strategy` 协议 +
  `CTAStrategy`(瘦策略,复用 `decide_targets`,默认 `hold_all=True` 剥离无效选股 PBO=0.67)+
  等权 `MultiStrategy`;`backtest(strategy=)` 决策来源可插拔(`strategy=None` 走 `decide_targets`
  直连,所有 `run_*` 零回归);K=1 口径守恒(`test_multi_k1_equivariance` + `multi` 子命令,
  真实数据 NAV 差异 0.00e+00)。`CTAStrategy` 用 `**decide_kwargs` 透传,默认值由 `decide_targets`
  唯一定,避免两处漂移。

- [ ] **[低] M2** 模块重组 / 改名(阶段2,触发条件:**有第二个策略**)— `etf_momentum.py`(1600 行)
  实为通用回测引擎、`momentum_core.py` 实为 CTA 逻辑,名实错位。计划拆 `engine`/`data`/`cta`/
  `diagnostics`/`cli` 等模块(= H3 单文件大重构,落到多策略语境)。**现在不做**:仅 CTA 一个策略
  时正确模块边界尚未显现(拆分是猜,第二个策略到来边界才清楚),且改名高扰动(5+ 文件 import/
  test/git/文档同步)。决策(2026-07-08):用户选"不改,记一笔"。

- [x] **M3** 阶段2 第一枪:国债时序动量 Strategy + K=2 组合(2026-07-08 落地)— 三候选相关性诊断
  (diag_mr / diag_pairs / diag_bonds)中,均值回归 +0.71 同源、配对 -0.04 互补但无 alpha,**国债
  -0.115 互补 + 夏普 1.51 + 组合夏普 1.29 > 单 CTA 1.01**——唯一通过"互补+自身+组合增益"三道关的方向。
  `BondMomentumStrategy`(国债混合动量>0 且价>MA200→满仓,否则空仓)实现 `Strategy` 接口;`run_multi`
  扩 K=2 + 国债占比 allocs 扫描。**关键坑**:backtest 的 `if target:` 把空 dict `{}` 当"无操作",故空仓
  必须返回 `{bond:0.0}`(非空 dict)才触发清仓——否则空仓信号失效、退化成买入持有(实测夏普 1.37→1.51)。
  实测:国债时序动量 夏普1.51/回撤-4.4%/Sortino2.30,与 CTA 相关性 -0.115(股债跷跷板);K=2 等权
  夏普1.29/回撤-7.5%(回撤砍半);allocs 扫描国债 75% 夏普峰值 1.69(⚠ 诊断用,部署等权 50/50)。
  **trade-off**:国债夏普高但绝对收益低(年化3.1%),K=2 把组合年化 11.4%→7.4%(用收益换稳健)。
  测试 19/19(新增 `test_bond_strategy_equivariance` atol1e-9 + `test_multi_k2_diversification`)。
  **下一步(阶段2)**:risk-parity-across-strategies 资金分配、策略间相关性矩阵、组合 PBO、策略级收益归因。

## 解决记录 (Changelog)

> 每完成一条,在此追加一行。建议:`日期 | 编号 | 改动摘要 | 验证方式 | 结果`

| 日期 | 编号 | 改动摘要 | 验证方式 | 结果 |
|------|------|----------|----------|------|
| 2026-07-04 | C1 | `decide_targets` 加 `weighting="inv_vol"`（权重∝1/σ，默认仍 equal）+ `INV_VOL_WINDOW=63` | 12 年真实数据 main A/B + 纯 Python 单元测试 | 夏普 1.21→1.23、回撤 -19.9%→-18.3%、波动 13.2%→12.6%，年化 -0.5pp。边际有效（主降回撤），疑与 vol_target 重叠 → 待配合 C3 / 关 vol_target 验证独立价值 |
| 2026-07-04 | F2 | 新增 `rolling_metrics()`（滚动年化夏普/波动）+ main 三子图 | main 滚动指标摘要 | 反向波动滚动夏普 均值 0.96/最差 -1.64/>0 占比 84%，优于等权 0.92/-1.72/81% |
| 2026-07-04 | F6 | 三子图加水下曲线（underwater drawdown） | main 画图 | 已加（与 F2 同图），等权 vs 反向波动回撤对比可视化 |
| 2026-07-05 | D1 | 统一成交口径到 T+1：核实 BT coc 与向量化 shift(1) 等价（同"月末当日收盘成交"），两版升级为"信号 T 日收盘→次日成交"；向量化 backtest 加 pending 延迟写 weights、BT 删 set_coc | 纯构造数据时序自检 6 项（全封涨停→空仓、按成交日而非信号日判定、跌停维持旧仓、limit_masks 分档、空mask==None 向后兼容）+ 语法检查 | 自检全过；真实数据净值复核待用户跑 `python etf_momentum.py`（本机数据源不可达：东财封代理 IP、tushare 无 token） |
| 2026-07-05 | D2 | 涨跌停三处过滤：`_limit`/`limit_masks`（未复权真实价，159915±20%/其余±10%）；向量化 cant_buy/cant_sell 掩码、BT `_at_limit`、实盘 build_orders 当日封板 | limit_masks 单元（分档正确）+ _apply_target_with_limits 单元（按成交日判定、封板方向维持旧仓）+ 全封涨停端到端（→空仓） | 自检全过；tushare 精确/akshare 近似 |
| 2026-07-05 | C1/C2 | D1/D2 新口径（T+1 成交 + 涨跌停）下复核 C1/C2 旧结论 | run_review（tushare，2013-03~2026-07 共 3225 日 / 8 只 ETF） | **结论与旧口径一致**：C2 全风控开/关 Δ夏普 +0.02、回撤 -24.8%→-23.7%（边际≈0、重叠，保持默认关）；C1 inv_vol 在 None/0.10/0.15 vol_target 下夏普 0/3 优于 equal（全输），保持默认 equal。**口径修正不改策略决策** |
| 2026-07-05 | F1 | `perf()` 加 Sortino（下行偏差作分母、MAR=0）/Calmar（年化/\|最大回撤\|）/Omega（正负收益和之比、阈值0）；`_print_table` 加三列；无风险/MAR/阈值统一按 0 | 单元数学（已知输入→Sortino 11.225/Omega 2.0、全正→下行偏差=0 钳极大）+ real 真实数据 12.7年 | **夏普掩盖信息**：反向波动 C1 夏普 0.81<等权 0.89，但 Sortino 2.22>2.00、Calmar 0.74>0.72、Omega 1.37>1.34（下行更稳）；不同指标下 C1 结论分歧、边际在噪声内，仍保持默认 equal |
| 2026-07-05 | A1 | 结案（代码已实现且默认关）——不再单列 | THR×CUT 网格（新浪 9 次触发）+ mini-robust | 单一"大盘急涨"信号无中间甜点：THR=10% 踏空（年化崩 12.5%）、≥15% 不触发；保持默认关，要可用需多信号（急涨+估值高位+波动飙升），列入后续 |
| 2026-07-05 | E2 | `_load_via_tushare` SDK→HTTP（`_ts_post` 服务商 POST，绕开 lxml 依赖链） | 本机 `python etf_momentum.py` real 模式端到端跑通 | real/review 子命令本机首次可跑（原 SDK `import tushare`→`import lxml` 即失败）；token 直接进请求体，不再受 SDK 环境变量干扰代理的坑 |
| 2026-07-05 | F3 | `regime_labels`（基准回撤>20%=熊、200 日均线分牛/震荡）+ `run_regime`（按 regime 切片 `_seg_perf`，带基准对照）；`regime` 子命令 + real 末尾两次拆解（等权/反向波动） | regime 子命令 + real 端到端（tushare 12.7 年） | **alpha 来源 = 熊市避损**：策略熊市年化 +1.3%（基准 -6.6%）、回撤 -13.1%（基准 -53.9%，仅 1/4）；牛市略输基准（19.4% vs 22.4%，轮动成本）；震荡弱项（Sortino -0.91）；反向波动熊市回撤 -11.6% 优于等权 -13.1% |
| 2026-07-05 | B1 | 试扩池至 14 只(+上证50/恒生/标普/消费/医药/原油/中概)→ 数字全降(年化 17.9→11.2、夏普 0.89→0.59、回撤 -24.8→-41.8) | 消融(踢医药恢复~60%)、时段分割(前段 26.3 vs 22.9 接近/后段 10.3 vs 3.8 拉开)、持仓级诊断(医药 -7.4%/年、消费 -3.5%/年长期下跌) | **回退**。伤害 80% 在 2019-2026 行业熊市;"原池更优"有用后段已知熊市判定的前视风险。原 7 只为经典动量资产选择,保持。**方法论教训**:池子成员只能基于先验(资产类别/上市年限/流动性),不能用回测收益挑(=过拟合) |
| 2026-07-05 | B2 | 试分档防御(动量≤0/vol_target→货基 511990、趋势/回撤→国债)→ 原 7 只年化 17.9→16.6(-1.3pp) | 对比回测 | **回退**。货基(~2%/年)收益<国债(样本期债牛),21 月动量≤0 切货基少赚。保持单一国债 |
| 2026-07-05 | H2 | `etf_momentum_live.py` QMT_PATH/ACCOUNT_ID → 三级回退(环境变量 > live_config.json > 占位),`_load_live_secret` 读取;占位时 connect_trader 拒连真实账户;`live_config.json` gitignore + 样例入库 | `_load_live_secret` 三级优先级单测(env>file>default)+ 半配置(仅配一项)被拦 + py_compile | 全过;凭证不入源码/不入 git,未配置时降级 paper/演示,不误连真实账户 |
| 2026-07-05 | A2 | `blended_momentum` 加 weights 参数(默认 None=等权)、`decide_targets`/`backtest` 加 mom_weights 透传;新增 `bootmom` 子命令(配对 block bootstrap) | A/B 5 方案 + bootstrap(tushare 12.7 年,全风控) | **保持默认等权**。长窗口加权夏普 0.89→1.0~1.06 但年化 17.9%→12.4~13% 腰斩、波动 21.3%→12.8% 腰斩;Sortino/Calmar/Omega 全降。主候选 Δ夏普+0.13 但 CI[-0.17,+0.47] 跨 0、P=0.39 不显著。夏普升是"降波动"伪改善(信号更黏偏防守、回撤没降→Calmar 反差)。省一个过拟合自由度,代码留 A/B。**注:此结论基于去污前数据,绝对值见重定基线说明** |
| 2026-07-05 | E3/E4 | 数据完整性修复(实现 G1 时由 γ3=27 暴露):① `_load_via_tushare` fund_div 按 ex_date 去重(tushare 返回重复分红行致过度复权,5 只 ETF 受影响);② `_SOURCE_SHIFT` 截断 510500/512100/513100 的 tushare 源永久断层前数据;③ `_data_quality_guard`(E4)清洗后残留不可成交伪迹告警,`load_real` 末尾自动调用 | DQ 守卫清洗后 0 伪迹;策略极端日 +59.28%→+4.00%、γ3 27→-0.98;py_compile + 单元 | **重定基线**:等权+全风控 年化17.9%→13.4%、波动21.3%→11.9%、回撤-24.8%→-17.6%、夏普0.89→1.15。伪跳变主要抬高波动/回撤非真 alpha。注:此前 E2"根治"(e112894)的"真复权"因 dup bug 并不真,本次才真修 |
| 2026-07-05 | G1 | `deflated_sharpe`(Bailey-LdP 2014)+ 纯 Python `_norm_ppf`(Acklam)/`_norm_cdf`(erfc)+ `dsr` 子命令 | 单元(norm 已知值/往返~1e-7 + DSR 极端 + SR_max 随 N 升 + 肥尾修正)+ 清洁数据 dsr | **清洁数据**:等权 SR=1.15 DSR@N=100=0.91、@N=10=0.99;反向波动 SR=1.20 DSR@100=0.93。N=100 最保守下略低于 0.95(边际可信)、N≤50 可信。核心策略经得起多重比较(与 boot/wf 互补)。污染版曾虚高 0.985 |
| 2026-07-05 | 重定基线清洁重跑 | C1/C2 bootstrap + F3/G1 复核(load_real 已 E3/E4 清洁,重跑子命令即可) | review/main/regime/dsr + 临时 bootstrap 脚本(inv_vol vs equal、drawdown_prot 开/关;n=2000,block=21) | **C1** Δ夏普+0.057 P=0.096 边际(3/3 网格全胜,污染期 1/9)、**C2** Δ夏普+0.035 P=0.21 但回撤-17.6%→-15.6%/Calmar 0.76→0.86 实质改善、**F3/G1** 稳健。两者清洁后明显改善但仍未跨显著阈值→默认开关不变;修正"相对结论大体稳健"判断(C1/C2 偏乐观)。机制:伪跳变抬高波动掩盖 inv_vol/C2 作用 |
| 2026-07-05 | E3 修正/pre_close 法 + B1 清洁重跑 | `_load_via_tushare` 用 pre_close 对齐除权/拆分(替代 fund_div+_SOURCE_SHIFT,因子 f[t]=pre_close[t]/close[t-1]);扩 ALL_CODES 拉 6 新标的(原油 tushare 不覆盖跳过)+`_with_pool` 跑 13 池对比 | 探测确认 pre_close 准(close/pre_close 全期 max≤10%)+ B1 13池vs7池 + review/dsr/bootstrap 重跑 | **第三次重定基线**:年化13.4%→15.1%、夏普1.15→1.20。E3 的 _SOURCE_SHIFT 是误判(510500/512100/513100 pre_close 实际准、恢复早期历史);pre_close 法覆盖份额拆分(159928/513500/512010)。**B1 稳健**:扩13池夏普1.20→0.94/年化-3.4pp/回撤不变,扩池有害保持原7只。C1 P=0.166保持equal、C2 P=0.112+回撤-15.6%本职强保持关、G1 DSR升(F3/G1/B1 相对结论稳健) |
| 2026-07-05 | A2/B2 pre_close 重跑 | bootmom(A2) + `decide_targets` 加 `defense_cash` 跑分档(B2) | bootmom A/B+bootstrap;B2 单一国债 vs 分档对比 | **A2**:主候选 Δ-0.030/P=0.675(污染期 +0.13 伪改善),长窗口加权只降年化,保持等权。**B2**:分档 -1.2pp,货基 511990 低估真实 ~2%(份额收益 tushare 不提供),方向不变保持单一国债。`decide_targets` 加 defense_cash(默认 None,代码留 A/B);货币 ETF 份额收益缺失是 tushare 局限 |
| 2026-07-05 | E5 | 审视起点锚定:`ready[0]`=BENCH+DEFENSE 首个共同非NaN日 → **无硬前视**(纯数据首日驱动;backtest `loc[:d]`+`shift(1)`+pending T+1+warm-up 全套防未来函数,D1/D2 自检全过);截到 ready[0] 是有意设计(保证起点核心池就绪)。抽 `_anchor_start(px)`(tushare/东财两处共用)打印审计行让隐性锚定显性化 | `_e5check.py` 调 load_real 实跑 + py_compile | `[E5 起点] ready[0]=2013-03-25;起点时 POOL 未就绪:[512100,518880,513100]`——8 只池里 3 只上市晚于起点,早期动量 dropna 自动跳过(非前视)。**无前视结论确立;不改截断逻辑、不重跑基线** |
| 2026-07-05 | C6 | 验证波动+趋势两套减仓叠加是否伤收益(4 配置 A/B + 3 个配对 block bootstrap) | `_c6check.py` 跑 backtest×4 + bootstrap n=2000/block=21(已删) | 叠加夏普1.198 > 仅vol 1.070 > 仅trend 0.586 > 都不开 0.534(回撤-20.2%/-28.0%/-49.2%/-55.2%);叠加 vs 都不开 Δ+0.664 P=0.000、加vol边际 Δ+0.612 P=0.000、加trend边际 Δ+0.127 P=0.043。**叠加不伤收益,两套互补(vol主力压波动+trend补回撤);保持默认全开,无需联合优化** |
| 2026-07-05 | H1 | 回测-实盘对账 `reconcile()`:模型 target 权重 vs 实盘持仓换同口径(含现金币种)逐币种比、偏离≥10%告警(>band 避免整手/静默区固有偏离误报);`main` 调仓前(抓跨月漂移/上次未执行)+ 调仓后(抓涨跌停/拒单跳单)两处接入,paper/qmt 两模式 | `_h1check.py` 三场景单元(一致/偏离/空仓)+ py_compile(已删) | 一致 max0.0%、偏离告警 510300低配-13pp/511010超配+14pp、空仓现金+99pp——逻辑全过。只读不改交易、不抛异常。真实价值待 qmt 实盘接入(paper 全成交总≈一致) |
| 2026-07-05 | E1 | 审视样本期单一:判定受数据硬限制不可完全解决,降级为已知局限 | ETF 池上市日核查 + E5 起点审计 | 池里多数标的 2011-2013 上市、起点钉 2013-03-25;2008 时 ETF 不存在→无法回测到 2008(强提前起点则 POOL 凑不齐 top_n→空仓扭曲)。现有 12.7 年已覆盖 2015/2018/2020/2021-24 多 regime(F3 验证稳健)。**降级 [高]→[低]、结案**;跨市场验证留作未来独立工程 |
| 2026-07-05 | F4 | 因子归因 `factor_attribution`(CAPM 单因子:beta/alpha/IR/R²,纯 Python OLS)+ `run_attribution`/`attrib` 子命令 | `_f4unit.py` 合成序列 CAPM 回收(n=5000)+ attrib 实跑 + py_compile(已删) | 策略年化15.1% vs 基准7.4%、超额+7.8%、beta=0.37(低暴露)、alpha+11.8%(纯alpha)、IR=0.33、R²=0.41;分解 超额≈alpha+11.8%+beta拖累-4.6%。单元回收 beta0.483(真0.5)/alpha10.0%(真10.1%)——数学正确 |
| 2026-07-05 | F5 | 换手率统计(`backtest` 加 `return_turnover`)+ 成本倍数扫描(0/0.5/1/2/3/5×) | `_f5check.py` 实跑 + py_compile(已删) | 年换手7.4x;成本拖累仅0.63%/年;翻倍夏普1.154(基线96%)、5x仍1.020/年化12.6%;Break-even 24.7x。**成本极不敏感,alpha 不是幻觉** |
| 2026-07-05 | G2 | Purged K-Fold CV(`purged_cv`+`pcv` 子命令;6 折非重叠 + purge21 + embargo21,AFML 标准) | pcv 实跑 + py_compile | 全样本内最优夏普1.24、样本外(6 purged 折 12.7年)夏普1.11/年化13.4%/回撤-20.1%,**衰减0.89**(远超0.6)→ 无显著过拟合;各折选参多为部署默认。与 G1/wf 三重互证 |
| 2026-07-05 | A3 | 验证 SKIP_RECENT 跳月(经典 12-1):扫描 {0,5,10,21,42}+配对 bootstrap | `_a3check.py` 实跑(已删) | skip=21 夏普1.100<1.198(Δ−0.097/P=0.718)、skip=5 略升1.229 但 P=0.356 不显著;无一显著优于0。**保持 0**(多窗口+ETF+月调+风控已覆盖短期反转,经典 12-1 skip 失效) |
| 2026-07-05 | 批量结案+降级 15 项 | A4/A5/A6/B3/B4/B5/C5/C8/D3/D4/G6/I2 审视结案(被 C1/B1/F5/H1/D1 等覆盖或超定位)+ H3/H4/H6 降级[中]→[低] | 引用已有验证 | 详见各条目 |
| 2026-07-05 | 轻量该做 6 项 | C7(vw=20 最优)、A7(SMA>EMA)验证保持默认;C3(max_weight cap)、D5(整手四舍五入)、C4(perf 加 VaR/CVaR)、F7(bench_6040)实现 | 各项单元/实跑 PASS | C7/A7/C3/D5 默认不变;C4/F7 评估能力已加 |
| 2026-07-05 | G3 PBO | pbo/run_pbo + pbo 子命令(Bailey-LdP 排名法) | pbo 实跑 | PBO=0.67(>0.5 警告):选参过拟合,但策略整体稳健(pcv 衰减0.89);印证用经验默认不挑参 |
| 2026-07-05 | H5+I1 | test_momentum.py(6/6 PASS)+ I1 本土因子对比 | test 跑 + _i1check(已删) | H5 固化回归保护;I1:动量轮动15.1% 远超等权全池11.0%/沪深300 7.4%/中证1000小盘2.1%(小盘被动13年−51%回撤,反常识) |
| 2026-07-06 | J1–J7 | 与主流策略对标的方法论评审:新开 7 条盲区(2高+3中+2低)。核心结论"夏普 1.2 被三结构性偏差(池前视/风控为主/债牛)抬高,PBO=0.67+DSR@100=0.935 已报警被合理化" | 对标 AQR/Moskowitz/Moreira-Muir/LdP 方法论 + 引用本项目 C6(0.534→1.198)/F3(熊市避损)/F4(beta0.37)/G1(DSR)/G3(PBO) 数字 | **不推翻策略,要求把 1.2 夏普当上界**;J1/J2 为新最高优先级开放项(跨市场验证 + 等权+风控不选股对照);J3–J7 方法论/工程跟进。进度 [高]0→2 [中]0→3 [低]21→23 |
| 2026-07-06 | J2 | 加 `nomomentum` 子命令 + `decide_targets` `hold_all` 参数(等权全池不选股,候选集与策略同时点一致) + `bootstrap_selection` 配对 block bootstrap + `test_hold_all` 单元 | nomomentum 实跑(tushare 12.7 年)+ test_momentum 8/8 PASS | **选股边际 Δ夏普 +0.18 但 P=0.148 不显著、回撤更差(−16.9%→−20.2%)**;对照单跑夏普 1.02 已远超 B&H 0.44 → 策略核心价值在"分散池+vol+trend"非选股。选股非零贡献但不可靠,等权全池版是更稳近替代。**J2 结案**,进度 [高]2→1 |
| 2026-07-06 | J1 | 跨市场验证框架:`crossmarket.py`(美股资产类别池 + `_us_globals` 换池 + `backtest`/`bootstrap_selection` 加 `trend_code` + 多源 loader stooq-PoW/yfinance/本地CSV + 四条对照 + bootstrap);`.gitignore` 加 `usdata/` | `test_crossmarket_globals_swap` 合成数据验证 + py_compile;本机实跑确认数据失败模式 | **逻辑层完工、9/9 单元过**;本机 IP 被 stooq(Access denied)/yahoo(429)封 + 无 pip 装 yfinance → 真实美股数据跑不动,loader 给清晰 `usdata/*.csv` 回退提示。**待用户在能联网机器跑** `pip install yfinance && python crossmarket.py`。J1 保持 [高] 开放(结果未得),进度不变 |
| 2026-07-06 | J1(结果) | miniconda(`~/miniconda3`,python3.13+pip)装 yfinance 取美股 23 年真实数据跑通 `crossmarket.py`;修 `run_crossmarket` 判读 bug(d_sel_eq 含风控不能当选股证据→改用 d_sel_hold);loader 源顺序改 yfinance 优先 | `crossmarket.py` 实跑(2003–2026,277 次调仓)+ test 9/9 | **风控跨市场成立**(等权+风控 回撤 −49.7%→−31.7%、夏普 0.70→0.84);**选股跨市场无效**(动量轮动 0.83 ≤ 等权+风控 0.84、Δ−0.01、**bootstrap P=0.560**,与 A 股 J2 一致);策略跑赢 SPY B&H(+0.16)纯靠降回撤非选股。**净结论:策略本质=风险管理的分散 ETF 组合,非动量策略**。**J1 结案**,进度 [高]1→0(全清) |
| 2026-07-06 | J4 | `run_bondstress`/`_synth_defense_series`/`bondstress` 子命令:把防守资产(国债 511010)收益替换为 0%/年、−2%/年(确定性日复利、隔离漂移)重算策略 | `etf_momentum.py bondstress` 实跑(tushare 13.3 年,真实国债 +2.9%/年)+ py_compile | **回撤控制机制跨债市稳健**:剔除债牛回撤仅 −20.2%→−22.8%(+2.5pp)、熊债(−2%)→−23.5%(+3.3pp)——机制靠"熊市挪进不跌的防守",非债牛(推翻原"失灵"假设)。**收益水平有债牛顺风** ~1.8pp/年、夏普 ~0.13、Calmar 0.75→0.58→0.53;但熊债下仍夏普 1.01 远超 B&H。**J4 结案**,进度 [中]3→2 |
| 2026-07-06 | J3 | `run_dsr` 加月频口径(`_monthly_returns`:日收益月内复利成月收益,n≈152)+ 日/月双表 + J3 对比 | `etf_momentum.py dsr` 实跑(tushare)+ py_compile | **月频 DSR@50=0.999 ≥ 日频 0.960**——原'日频夸大可信度'假设被推翻(月收益聚合近正态、肥尾修正变小反令 SR_max↓→DSR↑)。两口径 N=10/50/100 全 >0.95(月频全 0.999),策略夏普 ~1.2 经得起更严月频检验。残留:月频仍按月独立,真'regime 独立'更小,已由 J1 美股佐证。**J3 结案**,进度 [中]2→1 |
| 2026-07-06 | J2(落地) | `hold_all` 做成实盘可选部署模式:`momentum_core.HOLD_ALL` 常量 + `etf_momentum_live._load_hold_all`(读 `live_config.json` 的 `hold_all` 键覆盖默认)+ main 透传并打印模式 + `live_config.example.json` 加示例 | `test_load_hold_all`(true/false/缺键三级回退)+ test 10/10 + py_compile | 改 `live_config.json` 的 `hold_all` 即可在'动量轮动'(默认)与'等权全池+风控'(J1/J2 证的稳健版:选股无可靠 alpha、回撤更浅)间切换,无需改代码。J2 结论从'代码留作 A/B 工具'升级为'可部署' |
| 2026-07-06 | J5 | `run_freezetest`/`freezetest` 子命令:连续跑策略在 2022-01-01 切开,按段(全样本/调参期/冻结期)切指标,比选股 vs 等权+风控 vs B&H | `etf_momentum.py freezetest` 实跑(tushare)+ py_compile | **PBO=0.67 警告被证实**:冻结期(2022–26)选股输给等权+风控(夏普 0.74<0.79、回撤 −15.0%<−10.8%)——全样本调出的选股近期段不成立,'经验默认'没消除选择偏差。但风控层冻结期仍跑赢 B&H +0.55 夏普、回撤砍到 1/3 → 过拟合集中在选股层、风控层稳健。DSR@100 担忧已由 J3 化解(月频 0.999)。**J5 结案,进度 [中]1→0([高]/[中] 全清)** |
| 2026-07-07 | F8 | `factor_attribution_multi(daily, px)` 多因子 OLS(MKT/SMB/VMG/BND/GLD/NSDQ 6 因子,从 `px` 构造、含截距 lstsq + t-stat + 调整 R² + 相关矩阵)+ `_factor_returns`/`_MFACTORS` + `run_mfattribution`/`mfat` 子命令(与 attrib 同源全风控) | `test_factor_attribution_multi_recovery` + `test_mfat_strips_spurious_alpha`(核心论点:真α=0+债金beta→CAPM假阳性、多因子剥回近0)+ test 12/12 + `etf_momentum.py mfat` 实跑(tushare n=3100) | **CAPM α+11.7%→多因子 α+7.0%,剥离+4.7%**(CAPM 误归为 alpha 的债/金/纳指/小盘 beta)。MKT β0.55/BND β0.22(t3.2)/GLD β0.13/NSDQ β0.13,R²0.41→0.53。**判读**:CAPM 高估 ~40%,但 +7.0% 真 alpha 存活(风控/择时在静态资产暴露外的附加值,印证 F3 非纯beta);caveat:6因子只剥资产类别 beta,未含动量/低波风格因子(策略本身属性),7.0% 是诚实下界非纯skill点估计。**F8 结案**,进度 总74→75/已完51→52 |
| 2026-07-08 | M1 | 新 `multi_strategy.py`(`Strategy` 协议 + `CTAStrategy` 瘦策略默认 `hold_all=True` + 等权 `MultiStrategy`)+ `backtest(strategy=)` 决策来源可插拔 + `run_multi`/`multi` 子命令 | `test_multi_k1_equivariance`(K=1 + K=2 守恒)+ test 17/17 + `etf_momentum.py multi` 实跑(tushare 12.7年) | 真实数据 NAV 差异 **0.00e+00**(策略路径逐点复刻 `decide_targets` 直连);CTA 因子化为独立 Strategy,阶段2 加第二个策略即进入组合层(资金分配/相关性/组合 PBO) |
| 2026-07-08 | M3 | `BondMomentumStrategy`(国债混合动量+MA200趋势;⚠ 空仓返回 `{bond:0.0}` 而非 `{}`,否则 backtest `if target` 当无操作→退化买入持有,夏普1.37→1.51)+ `run_multi` 扩 K=2 + 国债占比 allocs 扫描 | `test_bond_strategy_equivariance`(atol1e-9)+ `test_multi_k2_diversification` + test 19/19 + `multi` 实跑 | 三候选诊断唯一通过三道关:国债 vs CTA 相关性 **-0.115**(股债跷跷板)、自身夏普**1.51**/回撤-4.4%、**K=2 等权组合夏普1.29 > 单CTA1.01 + 回撤-16.9%→-7.5%砍半**;allocs 国债75%夏普峰值1.69(诊断用,部署等权);trade-off:组合年化11.4%→7.4%(用收益换稳健) |

> 注：C1 参数网格稳健性（mini-robust，Part2/3）因 akshare/东财接口限频未跑完；数据缓存 + 重试机制已就位（`tmp/px_cache.pkl`），接口冷却后可一键补跑。

---

## 参考文献(可查证)

- Asness, C. (1997). *The Interaction of Value and Momentum Strategies.* — 风险调整动量(动量/σ)
- Moskowitz, Ooi, Pedersen (2012). *Time Series Momentum.* — 时序动量、波动缩放头寸
- Moreira, Muir (2017). *Volatility-Managed Portfolios.* — 波动管理与组合层降仓
- Daniel, Moskowitz (2013). *Momentum Crashes.* — 动量崩溃的 regime 保护(对应 A1)
- Bailey, López de Prado (2014). *The Deflated Sharpe Ratio.* — 多重比较下的夏普调整(对应 G1)
- López de Prado (2018). *Advances in Financial Machine Learning.* — Purged CV / PBO(对应 G2/G3)
