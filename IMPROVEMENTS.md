# 策略改进清单 (Improvements Backlog)

> 来源:与业界成熟策略对标后的差距清单。对标对象:
> Asness/AQR(动量与多因子)、Moskowitz-Ooi-Pedersen(时序动量)、
> Moreira-Muir(波动管理)、Daniel-Moskowitz(动量崩溃)、
> 桥水(风险平价)、AHL/Winton(CTA)、López de Prado(回测过拟合检验)。
>
> **共 67 条**:[高] 14(定位内应补)/ [中] 33(可选)/ [低] 20(超出当前定位)

## 用法

- 解决一条就把 `- [ ]` 改成 `- [x]`,并在文末「解决记录」表补一行。
- 编号格式 `A1 / C2 / H1`,稳定不变,便于在 commit / PR / 日志里引用。
- 重新评估优先级时,直接改方括号里的 `[高]/[中]/[低]`,并在记录表说明原因。

## 图例

- **[高]** 定位内应补,优先做(决定回测可信度或几乎确定有效的"免费午餐")
- **[中]** 可选改进,按需
- **[低]** 超出当前定位 / 锦上添花

## 进度

```
总 67  ┃ 已完成 14 ┃ 待办 53        已完成: C1, F2, F6, E2, C2, D1, D2, F1, A1, F3, B1, B2, H2, A2
[高] 2  ┃ [中] 31  ┃ [低] 20
```
（每完成一批,更新上面三组数字)

---

## 建议执行顺序(决定成败的 6 处,先做这些)

很多条目互相替代,真正"决定成败"的是下面 6 处——要么修正回测可信度,要么是几乎确定有效的免费午餐:

1. **C1** 反向波动加权(代替等权)——几行代码,确定改善
2. **C2** 叠加回撤控制——补波动率目标的"慢刀阴跌"盲区
3. ~~**A1** 动量崩溃保护~~ ——已验证:单一"大盘急涨"信号无中间甜点(THR=10% 踏空、≥15% 不触发),默认关结案
4. ~~**F3** 分 regime 表现~~ ——已完成:alpha 来源是熊市避损(策略熊市回撤仅基准 1/4);评估指标组(F1/F2/F3/F6)全部完成
5. **D1+D2** 统一成交口径 + 处理涨跌停——不修这俩,回测数字不可信
6. ~~**B1+B2** 扩池 + 分档防御~~ ——已验证有害(扩池撞 2019 后行业熊市、分档货基<国债),且选池有前视风险,保持原 7 只 + 单一国债

---

## A. 信号与因子层 `momentum_core.py`

- [x] **[高] A1** 动量崩溃保护 — `decide_targets` 第五步,**默认关,验证后不建议启用**。触发效果(新浪,9次):后21日大盘涨6/跌3,真顶部(2015 股灾 5178 / 2024-09 / 2022-06)有救命但短期多踏空。**调参最终结论(THR×CUT 网格)**:THR=10%→夏普 0.96 但年化崩 12.5%(频繁砍仓踏空);THR≥15%→触发锐减、效果≈不开(=没用)。**无中间甜点**:单一"大盘急涨"信号在 A 股样本下要么过敏感踏空、要么不触发。保持默认关;要可用需多信号触发(急涨+估值高位+波动飙升),列入后续(结案:代码已实现且默认关,结论是不启用,不再单列)
- [x] **[高] A2** 混合动量各窗口等权平均 — **已实现+验证,保持默认等权**。`blended_momentum` 加 `weights` 参数(默认 None=等权,向后兼容)、`decide_targets`/`backtest` 加 `mom_weights` 透传;新增 `bootmom` 子命令(`bootstrap_mom_weights` 配对 block bootstrap)。**A/B + bootstrap 结论(tushare 12.7 年,全风控口径)**:长窗口加权((1,2,3)/(1,2,4 半衰期式)/(∝长度)/(1,1,3))**夏普 0.89→1.00~1.06 升,但年化 17.9%→12.4~13.0% 腰斩、波动 21.3%→12.8% 几近腰斩**;Sortino/Calmar/Omega 全降(1.99→1.38 / 0.72→0.53 / 1.34→1.23)。主候选(∝长度)Δ夏普 +0.13 但 **95%CI [-0.17,+0.47] 跨 0、P(Δ≤0)=0.39 不显著**。**关键判读**:夏普上升是"降波动"伪改善——长窗口主导使信号更黏、更偏防守(波动砍半主要砍的是上行),回撤几乎没降(-23.6% vs -24.8%)→ Calmar 反而更差。省一个过拟合自由度,保持默认等权,代码留作 A/B 工具(与 RISK_ADJ/C1 同类结案)
- [ ] **[中] A3** `SKIP_RECENT=0` 默认不跳最近一月 — 位置:`momentum_core` L32 — 经典 12-1 动量必跳最近一月;可在 `robust` 里补 skip 维度重测
- [ ] **[中] A4** `RISK_ADJ` 用最长窗口的日波动做分母 — 位置:`blended_momentum` L109 — 应按各窗口对应波动或全程波动,口径可商榷,可重检验
- [ ] **[中] A5** 信号无统计显著性(t-stat) — 位置:`decide_targets` 排序 — Moskowitz 时序动量用信号显著性阈值,而非纯排序
- [ ] **[中] A6** 无信号确认(MACD/突破/多均线共振) — 位置:全局 — 单一涨幅排序易被假突破骗
- [ ] **[中] A7** 趋势均线只用 SMA — 位置:`_below_trend` L150 — 业界常用 EMA / 自适应均线(KAMA/FRAMA)
- [ ] **[低] A8** 单因子(动量),无价值/carry/质量 — 超出"动量轮动"定位
- [ ] **[低] A9** 无动量"加速度"(趋势的趋势) — 二阶信号作辅助过滤

## B. 标的池与资产配置

- [x] **[高] B1** 池太小(7 选 3),集中度高 — **已验证:扩池有害,保持原 7 只**。试加 7 只(上证50/恒生/标普/消费/医药/原油/中概→14 池):全期年化 17.9%→11.2%、回撤 -24.8%→-41.8%、夏普 0.89→0.59。**时段分割揭示伤害 80% 在 2019-2026**(前段 26.3 vs 22.9 接近、后段 10.3 vs 3.8 拉开),即医药(集采)/消费/中概/恒生集中熊市。**前视教训**:不能用全期回测收益挑池子成员(=用结果选原因);"原池更优"主要靠后段行业熊市,结论样本依赖。原 7 只(4 宽基+红利+黄金+纳指,全正收益)是经典时序动量资产选择,先验上更基础,保持
- [x] **[高] B2** 防守资产单一(仅国债 511010) — **已验证:分档有害,保持单一国债**。试分档(动量≤0/vol_target 挪货基 511990、趋势/回撤挪国债):原 7 只下年化 17.9%→16.6%(-1.3pp)。根因:货基(~2%/年)收益 < 国债(2014-17 债牛+利率长下),21 月动量≤0 切货基少赚;同样样本依赖(国债恰好遇债牛)。先验上分档有流动性逻辑但样本不支持,保持单一国债
- [ ] **[中] B3** 池选择的存活偏差风险 — 位置:`POOL` — 已做 `universe` 消融,但"为何是这 7 只"本身仍需审视
- [ ] **[中] B4** 缺低相关资产 — 位置:`POOL` — 港股/日经/欧股/REITs/原油/铜/可转债,降组合相关性
- [ ] **[中] B5** 未考虑标的流动性/容量 — 位置:`POOL` — 中证1000 ETF 早期流动性差,大资金有冲击成本
- [ ] **[低] B6** 无行业/风格敞口拆分 — 全是 beta,没拆成长/价值/大小盘暴露

## C. 组合构建与风控

- [x] **[高] C1** 等权加权而非风险平价/反向波动 — 位置:`decide_targets` — **已实现(默认仍 equal)**:加 `weighting="inv_vol"` + `INV_VOL_WINDOW=63`。**mini-robust 结论(新浪不复权数据,方向可信)**:① 关 vol_target 后 inv_vol 夏普仍=等权(0.58=0.58)→ **否定"与 vol_target 重叠"假设**;② 参数网格 inv_vol 夏普≥等权仅 1/9(11%),与东财复权下 +0.02 结论相反、且都在 ±0.05 噪声内 → **C1 无可靠夏普提升、不稳健,保持默认 equal**。代码保留作 A/B 工具
- [x] **[高] C2** 回撤控制 — `decide_targets` 第六步,默认关。**tushare 复权验证**:① 单独有效(关其它风控:回撤 -49.5%→-42.6%,夏普 0.54→0.59);② **但全风控下被 vol_target+trend 覆盖**(开/关 C2:夏普 0.81/0.83、回撤均 -25.6%,边际≈0)。与 C1 同类:单独有效但与现有风控重叠。保持默认关;若不用 vol_target/trend 可作替代降回撤
- [ ] **[中] C3** 无单标的集中度上限 — 位置:`decide_targets` — 仅 `cash_buffer=0.99` 全局上限,缺个股权重 cap
- [ ] **[中] C4** 无尾部风险约束(CVaR/VaR) — 极端行情保护缺失
- [ ] **[中] C5** 相关性矩阵未显式建模优化 — 位置:`_realized_vol` L116 — 隐含等权组合波动,未用协方差做风险预算
- [ ] **[中] C6** 两套减仓(波动+趋势)简单叠加 — 位置:`decide_targets` L204-224 — 无统一优化框架,未测叠加是否伤害收益
- [ ] **[中] C7** `VOL_WINDOW=20` 估计波动噪声大 — 位置:L41 — 业界常用 60 日或 EMA 平滑
- [ ] **[中] C8** 趋势过滤单向(只看跌破砍仓) — 位置:`_below_trend` — 不看突破回补/加仓,信号利用不全
- [ ] **[低] C9** 无止损(trailing/fixed stop) — 盘中黑天鹅只能等月末
- [ ] **[低] C10** 纯多头,无对冲 — 只能缩进国债,熊市不能做空盈利

## D. 调仓与执行

- [x] **[高] D1** 统一成交口径到 T+1 — **已实现**。核实发现 BT `set_coc` 与向量化 `shift(1)` **本质等价**(都是"月末当日收盘成交",原"shift(1) 更严谨"为误判)。两版统一升级到 **T+1 成交**(信号 T 日收盘 → 次日成交):向量化 `backtest` 加 pending 缓冲延迟写 weights、BT 删 `set_coc`(默认 next-bar 开盘成交)。消除"收盘价信号+收盘价成交"的乐观偏误。时序自检 6 项全过(见解决记录)
- [x] **[高] D2** 涨跌停处理 — **已实现(三处)**。共用 `_limit`/`limit_masks`(momentum_core:159915±20%、其余±10%)用未复权真实价算"收盘封板":向量化 `backtest` 加 `cant_buy/cant_sell` 掩码在成交日(T+1)过滤、BT `next` 加 `_at_limit` 下单前跳过、实盘 `build_orders` 加当日封板过滤。封板方向维持原仓、滞留权重落现金。tushare 路径用 fund_daily 的 pre_close 精确判定,akshare 回退用复权环比近似
- [ ] **[中] D3** 滑点固定 5bp,未按流动性动态 — 位置:`SLIPPAGE` L43 — 创业板/1000 真实冲击成本更高
- [ ] **[中] D4** `paper_broker` 同步全成 — 位置:`paper_broker.py` L51 `order` — 无部分成交/拒单/断连模拟,实盘鲁棒性未验证
- [ ] **[中] D5** 整手向下取整 — 位置:`etf_momentum_live.py` L134 `// LOT` — 小幅欠仓长期累积,可用"四舍五入 + 现金兜底"
- [ ] **[低] D6** 无拆单(TWAP/VWAP) — 小资金无所谓
- [ ] **[低] D7** 无订单成交确认/状态追踪 — 下单后不核成交

## E. 数据与样本

- [ ] **[高] E1** 样本期单一(仅 2014-2025 A 股) — 缺 2008/极端熊市 stress test;未跨市场验证泛化
- [x] **[中] E2** 单一数据源 akshare — **已根治**:load_real 集成 tushare(`_load_via_tushare`),走 fund_daily+fund_div 手动前复权(pro_bar adj 对 ETF 不生效),配 `TUSHARE_TOKEN`/`TUSHARE_API` 环境变量自动启用(或本地 `.tushare_token` 文件回退),否则回退东财(带重试)。tushare 不限频、复权准确,是唯一稳定可靠的 ETF 复权源。**2026-07-05 SDK→HTTP 加固**:`_load_via_tushare` 原用 tushare SDK,本机 termux 编译 lxml 不过致 `import tushare` 即失败、real/review 子命令本机跑不通;改用服务商 HTTP POST(`_ts_post`,绕开 lxml 链),real/review 本机首次跑通
- [ ] **[中] E3** 前复权跳变影响动量计算 — 位置:`load_real` `adjust="qfq"` — 未做复权点清洗
- [ ] **[中] E4** 无数据质量监控 — 缺失/异常无告警
- [ ] **[中] E5** 起点锚定逻辑 — 位置:`load_real` L46 `ready` — 有微弱前视风险,需审视
- [ ] **[低] E6** 无分红/税收处理 — 总收益略偏离真实
- [ ] **[低] E7** 停牌 ffill 未标记 — 合理但可标记区分

## F. 评估指标 `perf()` `etf_momentum.py` L117

- [x] **[高] F1** `perf()` 仅 5 指标,缺 Sortino/Calmar/Omega — **已实现**:`perf()` 加 Sortino(下行偏差作分母、MAR=0)、Calmar(年化/|最大回撤|)、Omega(正负收益和之比、阈值0,不年化),`_print_table` 加三列。无风险/MAR/阈值统一按 0(与现有夏普口径一致)。**新指标揭示夏普掩盖的信息**:12.7 年真实数据下反向波动 C1 夏普低于等权(0.81<0.89)但 Sortino/Calmar/Omega 均更高(2.22>2.00 / 0.74>0.72 / 1.37>1.34)→ 反向波动"下行更稳",印证 C1 不稳健(不同指标下结论分歧、边际在噪声内,仍保持默认 equal)
- [x] **[高] F2** 缺滚动指标(滚动夏普/滚动回撤) — 全期一个数掩盖"某段靠运气",与过拟合哲学矛盾 — **已完成**:新增 `rolling_metrics()`(滚动年化夏普/波动)+ main 三子图。反向波动滚动夏普均值 0.96/最差 -1.64/>0 占比 84%,优于等权 0.92/-1.72/81%
- [x] **[高] F3** 缺分 regime 表现(牛/熊/震荡) — **已实现**:`regime_labels`(基准沪深300 回撤>20%=熊、站上 200 日均线=牛、其余震荡,刻画市场状态与持仓无关)+ `run_regime`(按 regime 切片 `_seg_perf` 重建净值,带基准同 regime 对照);新增 `regime` 子命令 + real 末尾两次拆解(等权/反向波动)。**揭示 alpha 来源**:策略熊市年化 +1.3%(基准 -6.6%)、回撤 -13.1%(基准 -53.9%,仅 1/4)——全期夏普 0.89 看着平庸,但收益几乎全来自熊市避灾;代价是牛市小幅跑输基准(19.4% vs 22.4%,轮动切换成本);震荡是弱项(Sortino -0.91)。反向波动熊市回撤 -11.6% 优于等权 -13.1%(印证 C1 下行更稳)
- [ ] **[中] F4** 缺因子归因(alpha-beta 分解、IR) — 跑赢基准多少是 alpha、多少是 beta 未拆
- [ ] **[中] F5** 缺换手率与成本敏感性 — 未测"成本翻倍后还赚不赚"
- [x] **[中] F6** 净值图无水下曲线(underwater plot) — 回撤持续期/恢复期不可视化 — **已完成**:三子图加水下曲线(等权 vs 反向波动回撤对比可视化)
- [ ] **[低] F7** 基准单一(只沪深300) — 应加中证全指、股债 60-40 对照

## G. 过拟合检验(已有基础,可加严)

- [ ] **[中] G1** 缺 Deflated Sharpe Ratio — Bailey-López de Prado(2014):按试验次数/样本/肥尾调整夏普,防多重比较
- [ ] **[中] G2** 缺 Purged/Combinatorial CV — López de Prado《AFML》:防标签泄漏的更严样本外
- [ ] **[中] G3** 缺 PBO(backtest overfitting 概率) — min-CPS 算过拟合概率,直接给"可信度分数"
- [ ] **[低] G4** `robust` 用 one-at-a-time — 缺二维交互扰动
- [ ] **[低] G5** `wf` grid 仅 8 个 — 覆盖有限
- [ ] **[低] G6** `boot` 只检了 RISK_ADJ — 其他改进(趋势/波动)未做显著性检验

## H. 工程与运营

- [ ] **[高] H1** 无回测-实盘对账(reconciliation) — 机构标配;实盘偏离回测无预警
- [x] **[高] H2** 凭证硬编码 — **已实现**:`etf_momentum_live.py` 的 `QMT_PATH/ACCOUNT_ID` 改三级回退(环境变量 `QMT_PATH`/`QMT_ACCOUNT_ID` > 本地 `live_config.json` > 占位值),经 `_load_live_secret` 读取、不写进源码;`live_config.json` 加 gitignore(样例 `live_config.example.json` 入库)。占位值时 `connect_trader` 拒绝连真实账户(防误操作)。与 `.tushare_token` 同一约定
- [ ] **[中] H3** 单文件脚本,无框架抽象 — 信号/组合/执行未分层,扩多策略难
- [ ] **[中] H4** 无监控/告警 — 实盘异常(断连/数据错/账户不一致)无通知
- [ ] **[中] H5** 无单元测试 — 改动无回归保护(自检脚本可固化为测试)
- [ ] **[中] H6** 参数硬编码 `momentum_core` — 应抽配置文件,便于实验对比
- [ ] **[低] H7** 用 `print` 无日志系统 — 缺分级日志、无落盘
- [ ] **[低] H8** 无实验跟踪 — 无 MLflow/wandb 式版本化
- [ ] **[低] H9** 状态文件无并发保护 — `paper_account.json` 并发写有竞态
- [ ] **[低] H10** 无回滚机制 — 下单出错无补救
- [ ] **[低] H11** 无绩效/归因报告自动生成 — 无 PDF/HTML 月报

## I. A 股本土化与制度

- [ ] **[中] I1** 未对比本土有效因子 — A 股动量 alpha 历史弱于美股,短期反转/小市值/低波更强,应做横向对比
- [ ] **[中] I2** T+1 制度约束未体现 — 当日买不能当日卖,极端行情调仓受限
- [ ] **[低] I3** 未考虑 ETF 折溢价(IOPV 偏离) — 折价买/溢价卖是额外收益源
- [ ] **[低] I4** 程序化交易报备仅注释 — 代码无报备状态检查(合规)

---

## 解决记录 (Changelog)

> 每完成一条,在此追加一行。建议:`日期 | 编号 | 改动摘要 | 验证方式 | 结果`

| 日期 | 编号 | 改动摘要 | 验证方式 | 结果 |
|------|------|----------|----------|------|
| 2026-07-04 | C1 | `decide_targets` 加 `weighting="inv_vol"`（权重∝1/σ，默认仍 equal）+ `INV_VOL_WINDOW=63` | 12 年真实数据 main A/B + 纯 Python 单元测试 | 夏普 1.21→1.23、回撤 -19.9%→-18.3%、波动 13.2%→12.6%，年化 -0.5pp。边际有效（主降回撤），疑与 vol_target 重叠 → 待配合 C3 / 关 vol_target 验证独立价值 |
| 2026-07-04 | F2 | 新增 `rolling_metrics()`（滚动年化夏普/波动）+ main 三子图 | main 滚动指标摘要 | 反向波动滚动夏普 均值 0.96/最差 -1.64/>0 占比 84%，优于等权 0.92/-1.72/81% |
| 2026-07-04 | F6 | 三子图加水下曲线（underwater drawdown） | main 画图 | 已加（与 F2 同图），等权 vs 反向波动回撤对比可视化 |
| 2026-07-05 | D1 | 统一成交口径到 T+1：核实 BT coc 与向量化 shift(1) 等价（同"月末当日收盘成交"），两版升级为"信号 T 日收盘→次日成交"；向量化 backtest 加 pending 延迟写 weights、BT 删 set_coc | 纯构造数据时序自检 6 项（全封涨停→空仓、按成交日而非信号日判定、跌停维持旧仓、limit_masks 分档、空mask==None 向后兼容）+ 语法检查 | 自检全过；真实数据净值复核待用户跑 `python etf_momentum.py`（本机数据源不可达：东财封代理 IP、tushare 无 token） |
| 2026-07-05 | D2 | 涨跌停三处过滤：`_limit`/`limit_masks`（未复权真实价，159915±20%/其余±10%）；向量化 cant_buy/cant_sell 掩码、BT `_at_limit`、实盘 build_orders 当日封板 | limit_masks 单元（分档正确）+ _apply_target_with_limits 单元（按成交日判定、封板方向维持旧仓）+ 全封涨停端到端（→空仓） | 自检全过；tushare 精确/akshare 近似 |
| 2026-07-05 | C1/C2 | D1/D2 新口径（T+1 成交 + 涨跌停）下复核 C1/C2 旧结论 | run_review（tushare，2013-03~2026-07 共 3225 日 / 8 只 ETF） | **结论与旧口径一致**：C2 全风控开/关 Δ夏普 +0.02、回撤 -24.8%→-23.7%（边际≈0、重叠，保持默认关）；C1 inv_vol 在 None/0.10/0.15 vol_target 下夏普 0/3 优于 equal（全输），保持默认 equal。**口径修正不改策略决策** |
| 2026-07-05 | F1 | `perf()` 加 Sortino（下行偏差作分母、MAR=0）/Calmar（年化/\|最大回撤\|）/Omega（正负收益和之比、阈值0）；`_print_table` 加三列；无风险/MAR/阈值统一按 0 | 单元数学（已知输入→Sortino 11.225/Omega 2.0、全正→下行偏差=0 钳极大）+ real 真实数据 12.7年 | **夏普掩盖信息**：反向波动 C1 夏普 0.81<等权 0.89，但 Sortino 2.22>2.00、Calmar 0.74>0.72、Omega 1.37>1.34（下行更稳）；不同指标下 C1 结论分歧、边际在噪声内，仍保持默认 equal |
| 2026-07-05 | A1 | 结案（代码已实现且默认关）——不再单列 | THR×CUT 网格（新浪 9 次触发）+ mini-robust | 单一"大盘急涨"信号无中间甜点：THR=10% 踏空（年化崩 12.5%）、≥15% 不触发；保持默认关，要可用需多信号（急涨+估值高位+波动飙升），列入后续 |
| 2026-07-05 | E2 | `_load_via_tushare` SDK→HTTP（`_ts_post` 服务商 POST，绕开 lxml 依赖链） | 本机 `python etf_momentum.py` real 模式端到端跑通 | real/review 子命令本机首次可跑（原 SDK `import tushare`→`import lxml` 即失败）；token 直接进请求体，不再受 SDK 环境变量干扰代理的坑 |
| 2026-07-05 | F3 | `regime_labels`（基准回撤>20%=熊、200 日均线分牛/震荡）+ `run_regime`（按 regime 切片 `_seg_perf`，带基准对照）；`regime` 子命令 + real 末尾两次拆解（等权/反向波动） | regime 子命令 + real 端到端（tushare 12.7 年） | **alpha 来源 = 熊市避损**：策略熊市年化 +1.3%（基准 -6.6%）、回撤 -13.1%（基准 -53.9%，仅 1/4）；牛市略输基准（19.4% vs 22.4%，轮动成本）；震荡弱项（Sortino -0.91）；反向波动熊市回撤 -11.6% 优于等权 -13.1% |
| 2026-07-05 | B1 | 试扩池至 14 只(+上证50/恒生/标普/消费/医药/原油/中概)→ 数字全降(年化 17.9→11.2、夏普 0.89→0.59、回撤 -24.8→-41.8) | 消融(踢医药恢复~60%)、时段分割(前段 26.3 vs 22.9 接近/后段 10.3 vs 3.8 拉开)、持仓级诊断(医药 -7.4%/年、消费 -3.5%/年长期下跌) | **回退**。伤害 80% 在 2019-2026 行业熊市;"原池更优"有用后段已知熊市判定的前视风险。原 7 只为经典动量资产选择,保持。**方法论教训**:池子成员只能基于先验(资产类别/上市年限/流动性),不能用回测收益挑(=过拟合) |
| 2026-07-05 | B2 | 试分档防御(动量≤0/vol_target→货基 511990、趋势/回撤→国债)→ 原 7 只年化 17.9→16.6(-1.3pp) | 对比回测 | **回退**。货基(~2%/年)收益<国债(样本期债牛),21 月动量≤0 切货基少赚。保持单一国债 |
| 2026-07-05 | H2 | `etf_momentum_live.py` QMT_PATH/ACCOUNT_ID → 三级回退(环境变量 > live_config.json > 占位),`_load_live_secret` 读取;占位时 connect_trader 拒连真实账户;`live_config.json` gitignore + 样例入库 | `_load_live_secret` 三级优先级单测(env>file>default)+ 半配置(仅配一项)被拦 + py_compile | 全过;凭证不入源码/不入 git,未配置时降级 paper/演示,不误连真实账户 |
| 2026-07-05 | A2 | `blended_momentum` 加 weights 参数(默认 None=等权)、`decide_targets`/`backtest` 加 mom_weights 透传;新增 `bootmom` 子命令(配对 block bootstrap) | A/B 5 方案 + bootstrap(tushare 12.7 年,全风控) | **保持默认等权**。长窗口加权夏普 0.89→1.0~1.06 但年化 17.9%→12.4~13% 腰斩、波动 21.3%→12.8% 腰斩;Sortino/Calmar/Omega 全降。主候选 Δ夏普+0.13 但 CI[-0.17,+0.47] 跨 0、P=0.39 不显著。夏普升是"降波动"伪改善(信号更黏偏防守、回撤没降→Calmar 反差)。省一个过拟合自由度,代码留 A/B |

> 注：C1 参数网格稳健性（mini-robust，Part2/3）因 akshare/东财接口限频未跑完；数据缓存 + 重试机制已就位（`tmp/px_cache.pkl`），接口冷却后可一键补跑。

---

## 参考文献(可查证)

- Asness, C. (1997). *The Interaction of Value and Momentum Strategies.* — 风险调整动量(动量/σ)
- Moskowitz, Ooi, Pedersen (2012). *Time Series Momentum.* — 时序动量、波动缩放头寸
- Moreira, Muir (2017). *Volatility-Managed Portfolios.* — 波动管理与组合层降仓
- Daniel, Moskowitz (2013). *Momentum Crashes.* — 动量崩溃的 regime 保护(对应 A1)
- Bailey, López de Prado (2014). *The Deflated Sharpe Ratio.* — 多重比较下的夏普调整(对应 G1)
- López de Prado (2018). *Advances in Financial Machine Learning.* — Purged CV / PBO(对应 G2/G3)
