# A 股 ETF 动量轮动策略 —— 实现逻辑详解

> 本文件是**策略实现**的完整说明：从数据到决策到执行，每一步的数学、参数、默认值、以及"为什么这么设"。
> 所有逻辑以代码为准（`cta.py` 决策大脑 / `engine.py` 回测 / `live.py` 实盘），本文是对代码的逐行解读。
> 代码是单一事实来源；若本文与代码冲突，以代码为准。

---

## 0. 架构总览：单一决策源

整个策略只有**一个决策入口**：`cta.decide_targets(recent_closes, ...) → (target, picks)`。

```
                    ┌─────────────────┐
   价格数据 ───────▶ │ cta.decide_targets │ ◀── 唯一决策大脑（6 步风控流程）
                    └────────┬────────┘
                             │ target = {code: 目标权重}
              ┌──────────────┴───────────────┐
              ▼                              ▼
     engine.backtest（回测）           live.py（实盘）
     连续权重 × 收益矩阵               整手订单 → 券商
     T+1 成交 + 涨跌停                 先卖后买 + 对账
```

**核心原则**：回测怎么选，实盘就怎么选。`backtest` 和 `live` 都调同一个 `decide_targets`，逻辑只存在一份，杜绝"回测一套、实盘另一套"的漂移。

**两种部署口径**（同一个 `decide_targets`，靠 `hold_all` 开关切换）：
- `hold_all=True`（**默认 + 实盘口径**）：等权全池 + 风控（vol/trend/C2），跳过选股。全期夏普 **1.08**、回撤 −12.4%、Calmar 0.93（2026-07-12 起默认；含 C2 回撤控制）。
- `hold_all=False`（诊断/对照用，`nomomentum` 子命令）：动量选股 top3 + 绝对动量切防守。全期夏普 1.19、回撤 −20.2%。**非默认**。
  - 切到 hold_all 的理由：项目自身研究（J1/J2/J5/G3）证明动量选股相对等权全池**无可靠 alpha**（A股 bootstrap P=0.148、美股 P=0.999 显著为负）、PBO=0.67 过拟合、冻结期选股夏普 0.74 < 等权+风控 0.79。选股是过拟合风险，等权更稳 → 默认改 hold_all。

---

## 1. 标的池与参数

### 1.1 标的池（`cta.py` L19-28，回测/实盘共享）

| 代码 | 名称 | 角色 | 起点(2013-03-25)就绪? |
|------|------|------|------|
| 510300 | 沪深300 | 宽基（兼基准 BENCH、趋势风向标） | ✓（2012-05 上市） |
| 510500 | 中证500 | 宽基（中盘） | ✓（2013-03 上市） |
| 159915 | 创业板 | 宽基（成长，±20% 涨跌停） | ✓（2011-09 上市） |
| 512100 | 中证1000 | 宽基（小盘） | ✗ 起点时未上市 |
| 510880 | 红利ETF | 价值/红利 | ✓（2007 上市） |
| 518880 | 黄金ETF | 与 A 股低相关的避险/趋势资产 | ✗ 起点时未上市 |
| 513100 | 纳指ETF | 海外权益，分散单一市场风险 | ✗ 起点时未上市 |
| **511010** | **国债ETF** | **防守资产 DEFENSE**（风险 off 时资金去处） | ✓（2013-03 上市） |

> "起点时未上市"由 `_anchor_start` 实证（`ready[0]=2013-03-25` 时打印 `起点时 POOL 未就绪: ['512100','518880','513100']`），这些标的早期在动量排序里被 `dropna` 自动跳过，不影响后期参与。

**池子设计的先验**：4 只宽基（大/中/小/成长）+ 红利（价值）+ 黄金 + 纳指（海外）= 经典时序动量资产选择。全正收益资产、跨市场低相关，A 股走弱时动量可轮动到黄金/纳指而非只能缩回国债。

**为什么是 7 选 3（或 hold_all 全池）**：B1 验证扩池有害（试加恒生/标普/消费/医药/原油/中概→夏普 1.20→0.94，2019 后行业熊市拖累）。原 7 只是"用结果选原因"的前视教训已记入方法论，保持。

### 1.2 全部参数（`cta.py` L31-116）

| 参数 | 默认值 | 作用 | 验证结论（保持默认的理由） |
|------|--------|------|---------------------------|
| `LOOKBACKS` | (21,63,126) | 混合动量回看窗口（约 1/3/6 月） | 多窗口平均比单一窗口稳 |
| `SKIP_RECENT` | 0 | 算动量跳过最近 N 日 | 扫描 skip∈{0,5,10,21,42}：skip=21（经典12-1）夏普 1.100 < skip0 的 1.198，无一显著优于 0 → 保持 0 |
| `RISK_ADJ` | False | 动量÷波动（风险调整动量） | bootstrap Δ≈+0.04、CI 跨 0、P(Δ≤0)≈0.22 不显著 → 关（省一个过拟合自由度） |
| `LOOKBACK_WEIGHTS` | None | 各窗口加权（None=等权） | 长窗口加权只降年化不提夏普（"降波动伪改善"）→ 保持等权 |
| `TOP_N` | 3 | 持有动量最高的前 N 只 | — |
| `CASH_BUFFER` | 0.99 | 目标仓位上限（留 1% 现金） | 吸收手续费/滑点 + 整四舍五入超买 |
| `HOLD_ALL` | True | 等权全池 vs 动量选股 | 2026-07-12 起默认 True（对齐实盘口径，见 §0）；False=选股诊断用 |
| `VOL_TARGET` | 0.15 | 年化目标波动 | 组合近期波动超标即缩仓。C6 验证：vol 是主力（压波动 20.4%→12.8%、夏普 +0.612 高度显著） |
| `VOL_WINDOW` | 20 | 估计近期波动的回看日 | 扫描 vw∈{10,20,40,60,90}：20 最优，长窗口反劣（缩仓不及时） |
| `COMMISSION` | 0.00025 | 单边手续费（2.5bp） | F5：成本极不敏感（Break-even 24.7×） |
| `SLIPPAGE` | 0.0005 | 单边滑点（5bp） | 同上 |
| `TREND_CODE` | "510300" | 趋势风向标（= BENCH） | 沪深300 本身在池里，数据现成 |
| `TREND_MA` | 200 | 趋势均线天数 | C6 验证：trend 是补充（把回撤 -28%→-20%、Calmar 0.52→0.75） |
| `TREND_CUT` | 0.5 | 下行时股票仓保留比例 | 0.5 = 砍半挪国债 |
| `WEIGHTING` | "equal" | 持仓加权（equal/inv_vol） | C1：inv_vol 夏普提升不稳健（1/9 网格、±0.05 噪声内）→ 保持 equal |
| `INV_VOL_WINDOW` | 63 | 反向波动加权估波动窗口 | 约 3 个月，比 20 日稳 |
| `CRASH_PROT` | **False** | 动量崩溃保护（A1） | 默认关：THR×CUT 网格无中间甜点（要么过敏踏空、要么不触发）。要可用需多信号 |
| `DRAWDOWN_PROT` | **True** | 回撤控制（C2） | 2026-07-12 起默认开（hold_all 口径 A/B：回撤 −16.9%→−12.4%、Calmar 0.68→0.93、夏普 1.01→1.08 P=0.122、年化几乎不掉）。详见 §3 第六步 |
| `MAX_LOOKBACK` | 126 | 最少历史长度 | = max(LOOKBACKS) |

> **关键判读**：`CRASH_PROT`/`RISK_ADJ`/`LOOKBACK_WEIGHTS`/`WEIGHTING=inv_vol` 都是"验证后判定不显著，保持默认关"——省过拟合自由度。默认**开**的风控：`VOL_TARGET` + `TREND_MA`（C6 验证叠加最优、P=0.000）+ `DRAWDOWN_PROT`（C2，2026-07-12 起开——hold_all 口径本职回撤/Calmar 强、P=0.122 未达 5% 但实盘目标函数重回撤故采纳，详见 §3 第六步）。

### 1.3 涨跌停档位（`cta.py` L112-126，回测/实盘共享）

```
_limit(code):
  显式表优先：159915（创业板）= 20%
  前缀兜底：588xxx（科创板）= 20%
  默认：主板/跨境 QDII/商品 = 10%
```
**不能纯按代码前缀推断**：159xxx 段混合（159915 创业板 20% vs 159941 跨境 10%），必须显式表。集中在此一处，回测向量化 / BT / 实盘三处共享，避免散落不一致。

---

## 2. 数据层（`engine.py`）

### 2.1 加载链路 `load_real`（L45）

```
load_real(with_limits)
  ├─ _load_via_tushare(with_limits)     ← 主力：tushare fund_daily + pre_close 前复权
  │    ├─ token: 环境变量 TUSHARE_TOKEN > 本地 .tushare_token 文件
  │    ├─ per-symbol 4 次重试 + 退避（E6）；失败 loud warning 跳过
  │    ├─ 关键标的兜底：BENCH/DEFENSE[0] 缺 → 东财 helper 补这 ≤2 个（非全量降级）
  │    └─ 全部失败 → 返回 None
  ├─ 若 tushare 返回 None → _load_via_eastmoney(with_limits)   ← 回退：akshare 东财 qfq
  └─ _data_quality_guard(px)            ← 清洗后残留不可成交伪迹（|日收益|>1.5×涨跌停）大声告警
```

**为什么主力是 tushare**：东财 fund_etf_hist_em 复权但限频；新浪不复权；baostock/股票接口不支持 ETF。tushare `fund_daily` 不限频、`pro_bar(adj='qfq')` 对 ETF 不生效故用 pre_close 手动前复权，是唯一稳定可靠的 ETF 复权源。

> **环境事实**：本机网络下东财 `push2his.eastmoney.com` 不可达（代理不通），tushare relay 经代理可用。故实盘/回测都走 tushare，akshare 仅作 tushare 全失败时的回退。

### 2.2 前复权：pre_close 对齐法（`_qfq_from_pre_close` L61）

ETF 有现金分红和份额拆分，必须前复权才能算连续动量。tushare `fund_daily` 返回未复权 close 和 pre_close。

```
f[t] = pre_close[t] / close[t-1]        # 偏离 1 = 除权/拆分（涨跌停时 pre_close=昨收，仅除权日 ≠1）
g[i] = ∏_{t≥i} f[t]                      # 累积因子
adj[i] = close[i] × ∏_{t>i} f[t] = close[i] × g.shift(-1)    # 最新价 = close 末值不变
```

替代了旧 `fund_div + _SOURCE_SHIFT` 方案（两缺陷：fund_div 不记录份额拆分；_SOURCE_SHIFT 误判源永久断层过度截断丢早期历史）。pre_close 法能正确处理份额拆分，恢复了 510500/512100/513100 的早期历史。

### 2.3 起点锚定（`_anchor_start` L252）

```
ready[0] = BENCH(510300) + DEFENSE[0](511010) 首个共同非 NaN 日  →  2013-03-25
```
- **无硬前视**：ready[0] 纯数据首日驱动，ffill 不改首日，不依赖收益。
- **为什么钉在 2013-03-25**：BENCH+DEFENSE 是锚定/防守，必须就绪。再提前则 POOL 多数未上市 → 动量凑不齐 top_n → 长期空仓扭曲统计。
- 起点时 POOL 未就绪：512100/518880/513100（上市晚于起点），动量排序 `dropna` 自动跳过。

### 2.4 有界 ffill（E7，L155/L194）

```python
px = pd.concat(series, axis=1).sort_index().ffill(limit=3).dropna(how="all")
```
只向前填 ≤3 交易日的短停牌 gap，更长 gap（停牌/未上市）留 NaN。避免无界 ffill 把停牌日填成平稳价、0% 收益喂给 `_realized_vol`（压低 vol → vol_target 不缩仓）和 `blended_momentum`（稀释趋势）。`cta.blended_momentum`/`_asset_daily_vol` 已 skip NaN，回测 `rets=px.pct_change().fillna(0.0)` 使停牌日显式 0 收益。

### 2.5 涨跌停掩码（`limit_masks` L203）

用**未复权真实价**算"收盘封板"掩码（前复权会扭曲涨跌幅）：

```python
cant_buy[code]  = close >= ROUND_HALF_UP(pre_close × (1 + limit))   # 涨停 → 买不进
cant_sell[code] = close <= ROUND_HALF_UP(pre_close × (1 - limit))   # 跌停 → 卖不出
```
**ROUND_HALF_UP 而非 numpy 的银行家舍入**：A 股官方按"分"四舍五入。`pc=0.95×1.1=1.045`，A 股涨停=1.05，numpy `round` 给 1.04 → close=1.04（未触板）被误判 cant_buy。用 `decimal.ROUND_HALF_UP` 修正 .x5 边界。

---

## 3. 决策核心：`decide_targets` 六步流程（`cta.py` L253）

> 输入：`recent_closes = {code: 收盘价序列(升序)}`，只需 POOL 里的标的。
> 输出：`target = {code: 目标权重}`（权重和 ≈ CASH_BUFFER=0.99），`picks = [中文名]`（日志用）。
> 候选标的历史不足 `max(LOOKBACKS)=126` 日 → `blended_momentum` 返回 None → 自动跳过。
> 可选标的 < top_n → 返回空 dict（空仓/保持现状，如回测初期）。

### 防御码集合（L299-304，B2 分档设计）

```python
dcode = DEFENSE[0]                      # 511010 国债（趋势/崩溃/回撤 的 proceeds 去处）
dcode_cash = defense_cash or dcode      # 动量≤0/vol_target 的 proceeds 去处（无 defense_cash 则=国债）
defense_codes = {dcode, dcode_cash}     # 集合（两者相等时即 {dcode}）
```
B2 分档：动量≤0 / vol_target 缩出的仓挪 `dcode_cash`（可配货基），趋势/崩溃/回撤缩出的仓挪 `dcode`（国债）。step3-6 的 `eq_codes`（"真正持有的股票"）过滤统一用 `defense_codes`——否则 `defense_cash != DEFENSE[0]` 时只排国债会漏排货基，导致货基被当股票缩放、proceeds 路由错乱（2026-07-11 审计修复的 bug）。

---

### 第一步：算每只候选标的的混合动量分（L285-295）

对 POOL 里每只标的调 `blended_momentum`，收集非 None 的 `(mom, code)`。

**`blended_momentum`（L154）数学**：
```
end = len(arr) - 1 - skip                          # 动量终点（跳过最近 skip 日）
for lb in (21, 63, 126):
    vals.append( arr[end] / arr[end - lb] - 1 )    # 各窗口涨幅
mom = weighted_mean(vals, weights)                 # 默认等权 = mean(vals)
# 可选 risk_adj: mom = mom / daily_vol(arr, end, max_lookback)
```
- 任一窗口历史不足（`end - lb < 0`）→ 整体返回 None（该标的本期不参与）。
- `weights=None`（默认）等权；序列则按 `Σ(wᵢ·vᵢ)/Σwᵢ`（负权截 0，全 0 退等权）。

### 第二步：构造目标权重 —— 选股 vs hold_all（L297-350）

#### `hold_all=False`（动量选股，研究口径）

```python
moms.sort(desc)                          # 按动量从高到低
picks_raw = moms[:top_n]                 # 取前 3

# 加权（equal / inv_vol）
if weighting == "inv_vol":               # C1：权重 ∝ 1/σ_i 归一化
    raw[code] = 1 / daily_vol(code, inv_vol_window=63)    # 波动数据不足 → 1 兜底（退等权）
    weights[code] = CASH_BUFFER × raw[code] / Σraw
else:                                    # equal（默认）
    weights[code] = CASH_BUFFER / top_n  = 0.33

for mom, code in picks_raw:
    if mom > 0:                          # 绝对动量为正 → 真持有
        target[code] += weights[code]
    else:                                # 动量≤0 → 这一份切防守（绝对动量过滤）
        target[dcode_cash] += weights[code]
```
**绝对动量过滤**（要点 2）：入选标的若混合动量≤0，该仓位切防守资产，避免普跌行情硬扛 → 控回撤。

**C3 单标的集中度上限**（`max_weight`，默认 None 关）：个股权重超 `max_weight` 的部分挪防守。等权 top_n=3 下单只≈33%，默认不触发。

#### `hold_all=True`（等权全池，默认 + 实盘口径）

```python
n_hold = len(moms)                       # 全部有足够历史的候选标的（沿用 step1 的 None 判定）
w_each = CASH_BUFFER / n_hold            # 等权
for _m, code in moms:
    target[code] += w_each               # 全部持有，不选股、不切防守
```
跳过选股（top_n 排序）和绝对动量（≤0 切防守），候选集与部署策略同时点一致（只差"选不选"），后续风控叠加原样运行。

### 第三步：波动率目标（L352-366）

```python
eq_codes = [c for c in target if c not in defense_codes]   # 真正持有的股票
if vol_target and eq_codes:
    rv = _realized_vol(recent_closes, eq_codes, vol_window=20)   # 持仓组合近 20 日年化波动
    if rv > vol_target:                  # 只在超标时降仓（不加杠杆）
        scale = vol_target / rv          # < 1
        for c in eq_codes:
            moved = target[c] × (1 - scale)
            target[c] ×= scale           # 缩股票仓
            target[dcode_cash] += moved  # 缩出的挪防守（B2→货基/国债）
```
**`_realized_vol`（L197）**：持仓组合近 `window` 日的日收益标准差 × √252 年化。
- 等权组合：组合日收益 = 各标的日收益的等权平均。
- `inv_vol` 加权时：传实际持仓权重 `rv_w`，组合日收益 = `Σ(wᵢ·rᵢ)/Σwᵢ`（使标定贴合实际持仓）。
- 已知近似：`WEIGHTING='inv_vol'` 且未传 weights 时，仍用等权口径估组合 vol——vol_target 标定在"等权 vol"上却作用在"inv_vol 权重"上，属粗略风控标定（inv_vol 默认关，风险量级一致）。

### 第四步：大盘趋势过滤（L368-376）

```python
if trend_ma and _below_trend(recent_closes, trend_code=510300, trend_ma=200):
    for c in eq_codes:                   # 此刻仍持有的股票（可能已被 vol 缩过）
        moved = target[c] × (1 - trend_cut=0.5)
        target[c] ×= 0.5                 # 砍半
        target[dcode] += moved           # 挪国债（注意：→dcode 国债，非 dcode_cash）
```
**`_below_trend`（L237）**：沪深300 最新收盘 < 其 200 日简单均线 → 大盘下行。数据不足/缺失保守返回 False（不误杀）。

**两套独立减仓机制可叠加**（C6 验证最优）：vol_target 管"波动太大"，trend 管"大盘方向向下"。两者都只减不加。叠加 vs 都不开 Δ夏普 +0.664（P=0.000 高度显著）；vol 是主力（+0.612），trend 是补充（主要把回撤 -28%→-20%）。

### 第五步：动量崩溃保护（A1，L378-390，默认关）

```python
if crash_prot:                           # 默认 False
    past, now = arr[-1-21], arr[-1]      # 风向标近 21 日
    if now/past - 1 >= 0.10:             # 月涨 ≥10% = 急涨（暴跌后暴力反转 = 动量崩溃前兆）
        for c in eq_codes:
            target[c] ×= 0.5; target[dcode] += moved
```
Daniel-Moskowitz 2013。与 vol/trend 正交（看"反弹速度"）。默认关：THR×CUT 网格无中间甜点（THR=10% 频繁踏空、THR≥15% 几乎不触发）。

### 第六步：回撤控制（C2，L392-404，默认开）

```python
if drawdown_prot:                        # 默认 True（2026-07-12 改）
    peak = max(arr[-126:]); now = arr[-1]
    if (now - peak)/peak <= -0.10:       # 大盘距半年高点跌 ≥10% = 慢刀阴跌
        for c in eq_codes:
            target[c] ×= 0.5; target[dcode] += moved
```
vol_target 的盲区是"慢刀阴跌"（低波动但持续下跌）。**2026-07-12 起默认开**：hold_all 口径 A/B + bootstrap——回撤 −16.9%→−12.4%（改善 4.5pp）、Calmar 0.68→0.93（+38%）、夏普 1.01→1.08（Δ+0.066、P=0.122 未达 5% 但本职回撤/Calmar 强）、年化几乎不掉（避损不踏空）。实盘目标函数重回撤/Calmar 而非夏普显著，故采纳。

---

## 4. 回测执行层：`engine.backtest`（L346）

### 4.1 成交口径 D1：T+1 成交、T+2 吃收益（防未来函数）

```
信号日 T（月末收盘）：  算 decide_targets(recent=loc[:T]) → pending
成交日 T+1：            _apply_target_with_limits(pending) → cur 落地（按 T+1 涨跌停过滤）
收益日 T+2 起：         w_lag = weights.shift(1)；gross = w_lag × rets
```
- **为什么 T+1 成交**：避免"收盘价信号 + 收盘价成交"的前视/乐观偏误。信号用 T 日收盘算，次日才成交。
- **shift(1) 自洽**：今天的收益用"昨天收盘时持有的权重"计，调仓日当天还按旧权重。

### 4.2 涨跌停落地 D2：`_apply_target_with_limits`（L323）

```python
# 不在 target 的旧仓：默认清零；当日封跌停（卖不出）则维持
# target 中 w>old 且封涨停 → 买不进，维持 old
# target 中 w<old 且封跌停 → 卖不出，维持 old
# 被过滤滞留的权重自然落到现金（现金无收益列 = 0 收益）
```

### 4.3 回测主循环（L382-389）

```python
weights = DataFrame(0, index=所有交易日, columns=所有标的)
cur = Series(0)                          # 当前实际持仓权重
pending = None
for d in px.index:
    if pending:                          # 今日 = T+1 成交日：落地 pending
        cur = _apply_target_with_limits(pending, cur, d, cant_buy, cant_sell)
        pending = None
    if d in 月末调仓日:                   # 今日 = T 信号日：算信号，延迟到次日
        recent = {c: px[c].loc[:d].dropna().tolist() for c in POOL}
        target, picks = decide_targets(recent, ...)
        pending = (target, picks)
    weights.loc[d] = cur
```

### 4.4 换手与成本（L378-381）

```python
w_lag = weights.shift(1).fillna(0)
gross = (w_lag × rets).sum(axis=1)              # 组合每日毛收益
turnover = (weights - weights.shift(1)).abs().sum(axis=1)   # 权重总绝对变动
cost = turnover × (COMMISSION + SLIPPAGE)       # 7.5bp 单边
net = gross - cost
```
**F5 实测**：年换手仅 7.4×（月均 0.61×），成本拖累 0.63%/年（毛 15.7%→净 15.1%，F5 历史 pre_close 基线口径；当前数据源漂移后净≈15.0%，见 §7.1）。Break-even ≈ 24.7×（单边 185bp 才吃光 alpha）。策略对成本极不敏感。

### 4.5 warmup 与净值（L368-389）

```python
need = max(MAX_LOOKBACK, max(lookbacks) + skip_recent)   # 默认 = 126
rebal_days = [d for d in 月末 if d >= px.index[need]]    # 跳过历史不足 need 的初期
nav = (1 + net).cumprod().loc[px.index[need]:]
nav = nav / nav.iloc[0]                                  # 归一化到 1.0
```

---

## 5. 实盘执行层：`live.py`

### 5.1 触发节奏（L347-354）

- `MONTH_END_ONLY=True`：只在"本月最后一个交易日"调仓（与回测月末口径对齐），`force` 参数可绕过。
- `already_done_this_month()`：本月已调仓则跳过（`mark_done_this_month` 写 `last_rebalance.txt`），`force` 可绕过。适合每日定时触发、每月只动一次。

### 5.2 主流程（L347-422）

```
1. get_recent_closes(POOL+DEFENSE, 200)          # 取每只最近 200 根（够 200 日趋势均线）
   └─ 优先 engine.load_real(tushare，与回测同源)→ akshare 回退
2. decide_targets(recent, trend_ma=200, hold_all=配置值)   # 调核心大脑
3. 涨跌停过滤：当日收盘封板的标的（环比近似）→ cant_buy_today/cant_sell_today
4. connect_trader()：BROKER=paper→本地模拟；qmt→miniQMT（凭证仍是占位则降级演示）
5. read_account()：总资产 + 当前持仓
6. reconcile(target, positions, when="调仓前")   # H1 对账：抓跨月漂移/上次未完整执行
7. build_orders(target, recent, total, positions, cant_buy/sell)   # 先卖后买
8. paper 或 非 DRY_RUN → place() 逐单下单；否则只打印计划
9. mark_done_this_month()
10. read_account() + reconcile(when="调仓后")    # H1 对账：抓涨跌停/拒单致未全成交
```

### 5.3 `build_orders`（L203）—— 订单生成的 5 个细节

1. **目标股数 = 四舍五入到整百**（D5）：`int(total × w / px / 100 + 0.5) × 100`。替代原向下取整（避免长期欠仓累积），`CASH_BUFFER=0.99` 留 1% 现金兜底小幅超买。
2. **静默区 band=5%**：双边都持有、仅小幅漂移、偏离金额 < 5%×总资产 → 不动（省手续费）。换信号（清仓/新建）照常执行。
3. **涨跌停跳单**（D2）：封涨停的买单、封跌停的卖单直接跳过（券商本也会拒单），维持原仓。
4. **先卖后买**：`return sells + buys`，保证先回笼现金再买。
5. **现金兜底**（deepcode-3）：四舍五入每只最多多买 50 股，top3 同时五入可超 1% 兜底 → 现金为负。逐笔买单按"可用现金 = 当前现金 + 卖单回笼（扣滑点）"截断股数（向下取整到 LOT），宁可小幅欠配不超支。

### 5.4 `reconcile`（L299）—— H1 回测-实盘对账

把模型 `target` 权重与实盘 `positions` 换算到同一口径（含 `__cash__` 现金币种）逐项比，偏离 ≥ 10% 告警：
- **调仓前**：实盘累积持仓 vs 当前模型 → 抓跨月价格漂移 / 上次调仓未完整执行 / 分红或手工操作。
- **调仓后**：刚执行的持仓 vs 本次 target → 抓涨跌停/拒单致下单未全成交（实盘最关键）。
- 只读对比、不改交易、不抛异常（对账失败不阻断调仓）。

### 5.5 paper vs qmt（`connect_trader` L124）

- `BROKER=paper`：`PaperTrader`（本地 JSON 持久化的模拟券商），同步撮合（手续费+滑点），跨月累积，行为贴近真实账户。
- `BROKER=qmt`：`xtquant.XtQuantTrader` 连华泰 miniQMT。凭证（QMT_PATH/ACCOUNT_ID）从环境变量 > `live_config.json` 读取；仍是占位值或未装 xtquant → 降级演示（**拒绝连真实账户**，H2 安全设计）。

---

## 6. 评估层（`engine.py`）

### 6.1 `perf`（L409）—— 绩效指标

| 指标 | 算法 | 口径 |
|------|------|------|
| 年化 CAGR | `nav[-1]^(1/years) - 1` | 几何平均 |
| 波动 | `daily.std × √252` | 年化 |
| 夏普 | `mean×252 / (std×√252)` | 无风险利率=0 |
| Sortino | 分母换下行偏差（仅惩罚负收益，MAR=0） | 戳穿"靠几次大涨堆出来的夏普" |
| Calmar | 年化 / \|最大回撤\| | 风险调整回撤 |
| Omega | 正收益和 / \|负收益和\|（阈值0，不年化） | >1 盈利侧占优 |
| VaR95/99 | 日收益 α 分位 | 尾部风险 |
| CVaR95/99 | 尾部条件期望 | 平均最大日损失 |

### 6.2 `regime_labels`（L685）—— 牛熊震荡判定

用**基准沪深300**（非策略净值）打标签，刻画市场 regime 本身、与持仓无关：
```
熊市 = 从历史高点回撤 > 20%
牛市 = 非熊 且 净值在 200 日均线之上
震荡 = 其余
```
**F3 发现**：alpha 来源是**熊市避损**（策略熊市年化 +4.4% vs 基准 -4.5%、回撤 -14.9% vs 基准 -52.2%）；牛市年化 12.1% 略输基准 13.6% 但夏普更高（回撤控制）；震荡微正。

### 6.3 因子归因（F4 CAPM / F8 多因子）

- **CAPM**（`factor_attribution`）：策略日收益对沪深300 回归，拆 alpha/beta。实测 beta=0.37（低市场暴露）、年化 alpha +11.8%。
- **多因子**（`factor_attribution_multi`）：6 因子 OLS（MKT/SMB/VMG/BND/GLD/NSDQ）。CAPM 高估了 ~40%（债/金/纳指 beta 被当 alpha），但 **+7.0% 真 alpha 在 6 因子控制下存活**——风控/择时在静态资产暴露之外仍有真实附加值。

---

## 7. 当前回测表现（2026-07-12 实跑，12.8 年）

> **默认口径 = hold_all + C2 开**（§0）：全期年化 11.5%、夏普 1.08、回撤 −12.4%、Calmar 0.93。下方 §7.1 为选股口径（`hold_all=False`，诊断/对照、非默认）；§7.2 为 hold_all 衰减（C2 关口径，开 C2 后回撤进一步改善，全期 −16.9%→−12.4%）。

### 7.1 选股口径（`hold_all=False`，诊断/对照，**非默认**）

| 配置 | 年化 | 波动 | 回撤 | 夏普 | Sortino | Calmar |
|------|------|------|------|------|---------|--------|
| 无风控 | 8.5% | 20.4% | -55.2% | 0.52 | 0.69 | 0.15 |
| +波动目标15% | 14.4% | 14.0% | -28.0% | 1.06 | 1.49 | 0.51 |
| +趋势过滤(等权) | 15.0% | 12.8% | -20.2% | 1.19 | 1.70 | 0.74 |
| +趋势(反向波动C1) | 15.0% | 12.7% | -21.1% | **1.20** | 1.72 | 0.71 |
| 买入持有沪深300 | 7.1% | 22.3% | -45.5% | 0.43 | 0.61 | 0.16 |

### 7.2 实盘口径（`hold_all=True`）近几年衰减

| 窗口 | 年化 | 夏普 | 回撤 | 超额 vs 基准 |
|------|------|------|------|--------------|
| 全周期 | 11.4% | 1.01 | -16.9% | +4.2pp |
| 近 3 年 | 13.1% | 1.07 | -10.8% | +3.0pp |
| 近 5 年（选股口径对照） | 7.6% | 0.76 | -15.3% | +7.0pp |

**近几年衰减是真实的**（选股口径近 5 年夏普 1.20→0.76，2021-2023 慢熊 + 震荡市年化 -8.5%），但 hold_all 口径衰减温和（近 3 年反而 1.01→1.07）。实盘预期按夏普 1.0 量级，不是 1.2。

---

## 8. 已知局限与待办

### 8.1 实盘 readiness P0 清单

- ✅ **P0-1 实盘口径 hold_all**：`live_config.json` 设 `hold_all=true`，paper 闭环验证通过（调仓后对账偏离 0.7%）。
- ✅ **P0-2 relay 鲁棒性**：BENCH/DEFENSE 拉败时东财兜底，不再 KeyError 崩盘。
- ⏳ **P0-3 ETF 折溢价建模（J7）**：513100 纳指 QDII 盘中溢价常 3-5%，月末收盘调仓可能买在溢价高点——回测完全没有的隐性成本。需先 paper 攒 IOPV 数据。

### 8.2 过拟合担忧的诚实回答

- **DSR 日频口径失守**：等效试验数 N 扩到 [200,500,1000] 后，日频 DSR 跌破 0.95（N=1000→0.772）；但**月频仍稳**（N=1000→0.986）。月度调仓对齐月频 DSR，这点 OK。
- **PBO=0.67**（选股层）：J5 冻结期证实选股参数过拟合。**实盘用 hold_all 规避选股层过拟合**。
- **样本期单一**（E1）：2013-至今 A 股一段，无法回测到 2008。已覆盖 2015 股灾/2018 贸易战/2020 疫情/2021-2024 核心资产调整多 regime。跨市场验证（美股 23 年）已做（J1），方法论泛化。

### 8.3 其余 23 条 [低] 待办（均不阻塞实盘）

定位外扩展（A8 多因子 / A9 动量加速度 / B6 风格敞口 / C9 止损 / C10 对冲）、工程优化（H3 框架抽象 / H4 监控 / H6 配置文件 / H7 日志 / H9 并发保护 / H10 回滚）、精度项（D6 拆单 / D7 成交确认 / E6 分红税 / E7 停牌标记 / I3 折溢价 / I4 程序化报备）——详见 `IMPROVEMENTS.md`。

---

## 附：运行命令

```bash
~/miniconda3/bin/python engine.py real        # 全周期回测（选股口径，出图）
~/miniconda3/bin/python bt_recent.py 3 hold   # 近 3 年窗口（hold_all 实盘口径）
~/miniconda3/bin/python bt_recent.py 5 iv     # 近 5 年（反向波动选股）
~/miniconda3/bin/python live.py               # 实盘（默认 paper + DRY_RUN，月末调仓）
~/miniconda3/bin/python live.py force         # 强制重跑（绕过月末/已调仓检查，验证用）
~/miniconda3/bin/python test_momentum.py      # 测试套件（28 项）
```
